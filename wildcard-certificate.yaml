apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: timourhomelab-wildcard-tls
  namespace: gateway
spec:
  secretName: timourhomelab-wildcard-tls
  issuerRef:
    name: cloudflare-issuer
  dnsNames:
  - "*.timourhomelab.org"  # Wildcard für alle Subdomains
  - "timourhomelab.org"    # Apex Domain