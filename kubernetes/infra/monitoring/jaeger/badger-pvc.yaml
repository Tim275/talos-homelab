apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jaeger-badger-storage
  namespace: jaeger-system
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block-enterprise
  resources:
    requests:
      storage: 10Gi