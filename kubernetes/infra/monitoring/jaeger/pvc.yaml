apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jaeger-badger-storage
  namespace: jaeger
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-cephfs  # Using Ceph filesystem for Badger
  resources:
    requests:
      storage: 10Gi