apiVersion: v1
kind: Namespace
metadata:
  name: monitoring


  ###kubectl get namespace <namespace> -o json | jq 'del(.spec.finalizers)' | kubectl replace --raw "/api/v1/namespaces/<namespace>/finalize" -f -
  #kubectl get namespace argocd -o json | jq 'del(.spec.finalizers)' | kubectl replace --raw "/api/v1/namespaces/argocd/finalize" -f -
  #kubectl get namespace monitoring -o json | jq 'del(.spec.finalizers)' | kubectl replace --raw "/api/v1/namespaces/monitoring/finalize" -f -