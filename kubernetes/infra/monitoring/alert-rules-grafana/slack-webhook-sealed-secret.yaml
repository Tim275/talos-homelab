apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: slack-webhook-url
  namespace: monitoring
  labels:
    app.kubernetes.io/managed-by: argocd
    release: prometheus-operator
spec:
  encryptedData:
    url: VERSCHLUESSELTER_STRING_HIER
  template:
    metadata:
      name: slack-webhook-url
      namespace: monitoring
    type: Opaque