apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app.kubernetes.io/managed-by: argocd

resources:
  - ns.yaml
  - project.yaml
  - application-set.yaml
  - jaeger/
  # - opentelemetry/  # Disabled: ArgoCD kann nicht --enable-helm für Kustomize+Helm