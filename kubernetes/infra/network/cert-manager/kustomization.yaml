apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cert-Manager with Cloudflare DNS validation
resources:
  - https://github.com/cert-manager/cert-manager/releases/download/v1.15.1/cert-manager.yaml
  - sealed-cloudflare-api-token.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: network-infrastructure