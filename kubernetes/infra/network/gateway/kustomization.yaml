apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 4: Gateway Resources (After all infrastructure is ready)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "4"

resources:
  - ns.yaml
  - sealed-cloudflare-api-token.yaml  # Cloudflare API token secret for cert-manager
  - cloudflare-issuer.yaml
  - certificate-timourhomelab.yaml
  - gateway.yaml
  - http-redirect-to-https.yaml
  # See BOOTSTRAP-FIX.md and run gateway-api-bootstrap-fix.sh after ArgoCD sync
