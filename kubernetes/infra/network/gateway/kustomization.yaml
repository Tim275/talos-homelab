apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 1: Envoy Gateway Controller + Gateway Resources
commonAnnotations:
  argocd.argoproj.io/sync-wave: "1"

# Envoy Gateway v1.2.0 + Gateway Resources (Infrastructure as Code)
resources:
  - envoy-gateway-install.yaml
  - ns.yaml
  - cloudflare-issuer.yaml
  - certificate-timourhomelab.yaml
  - gateway.yaml
  - http-redirect-to-https.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: network-infrastructure