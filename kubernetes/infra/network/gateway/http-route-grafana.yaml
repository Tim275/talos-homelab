apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana-route
  namespace: monitoring
spec:
  parentRefs:
    - name: external
      namespace: gateway
  hostnames:
    - grafana.timourhomelab.org
  rules:
    - backendRefs:
        - name: grafana
          port: 80
