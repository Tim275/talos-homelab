# TEMPORARY: ArgoCD direkt über LoadBalancer bis Gateway funktioniert
# PLAN: Später wieder auf Gateway umstellen wenn Cilium Controller läuft
#
# HTTPRoute für ArgoCD - Verbindet Gateway mit ArgoCD Service  
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: argocd-route
#   namespace: argocd
# spec:
#   parentRefs:
#     - name: cilium-gateway
#       namespace: gateway
#   hostnames:
#     - "argo.timourhomelab.org"
#   rules:
#     - matches:
#         - path:
#             type: PathPrefix
#             value: /
#       backendRefs:
#         - name: argocd-server
#           port: 80

# AKTUELL: DNS Record zeigt direkt auf ArgoCD LoadBalancer
# DNS: argo.timourhomelab.org → 192.168.68.150