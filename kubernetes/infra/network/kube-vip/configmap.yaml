apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-vip-config
  namespace: kube-system
data:
  # VIP Configuration
  vip-address: "192.168.68.100"
  vip-interface: "eth0"
  vip-arp: "true"
  
  # Control plane configuration
  control-plane-endpoint: "192.168.68.100:6443"
  
  # Cluster information
  cluster-name: "homelab-k8s"