config:
  clusters:
    - url: https://192.168.68.100:8006/api2/json
      insecure: true
      token_id: "kubernetes-csi@pve!csi"
      token_secret: "your-csi-token-secret"
      region: homelab

storageClass:
  - name: proxmox-csi
    storage: local-zfs
    reclaimPolicy: Delete
    allowVolumeExpansion: true

node:
  tolerations:
    - operator: Exists

controller:
  tolerations:
    - operator: Exists