apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - application.yaml

metadata:
  name: mongodb-operator
  annotations:
    platform.config/component: "mongodb-operator"
    platform.config/tier: "controllers"
    platform.config/description: "MongoDB Community Operator - database controller/operator"