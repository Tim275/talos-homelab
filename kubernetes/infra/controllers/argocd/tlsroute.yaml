apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TLSRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  parentRefs:
    - { name: external, namespace: gateway, sectionName: tls-passthrough }
  hostnames: [ argo.timourhomelab.org ]
  rules:
    - backendRefs: [ { name: argocd-server, port: 443 } ]