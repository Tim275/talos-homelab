apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # VolumeSnapshot CRDs for Velero and CSI snapshot support
  - volume-snapshots.yaml
  
  # Prometheus Operator CRDs for monitoring and alerting
  - prometheus-operator-crds.yaml