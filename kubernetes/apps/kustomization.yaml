apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 25: Enterprise Applications
commonAnnotations:
  argocd.argoproj.io/sync-wave: "25"

namespace: argocd

# 🏢 Enterprise ApplicationSets - Clean Structure
resources:
  - audiobookshelf.yaml
  - n8n.yaml
  - kafka-demo.yaml
  - quantlab/applicationset.yaml
  # Add more enterprise apps here

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  team: timour
  managed-by: timour