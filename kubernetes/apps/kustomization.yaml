apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 25: Enterprise Applications
commonAnnotations:
  argocd.argoproj.io/sync-wave: "25"

namespace: argocd

# 🏢 Enterprise ApplicationSets - Clean Structure
# 🏢 Apps only contain environments/ - Applications moved to components/
# This follows Google/AWS GitOps best practices for enterprise scale

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  team: timour
  managed-by: timour