apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🚀 APPLICATIONS - User Applications Deployment
# Einfache, direkte Application-Definitionen ohne Matrix-Generatoren

namespace: argocd

resources:
  - project.yaml              # ArgoCD project für applications

# Zukünftige Applications hier hinzufügen:
# - webapp1.yaml             # Erste Web Application  
# - api-service.yaml         # API Service
# - database.yaml            # Database Application

commonLabels:
  tier: applications
  managed-by: kustomize-direct
  
commonAnnotations:
  apps.platform/version: "1.0"
  apps.platform/deployment-method: "direct-applications"