apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-data
  namespace: n8n-prod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block-enterprise
  resources:
    requests:
      storage: 2Gi