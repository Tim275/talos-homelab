apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Professional Quantitative Research Lab
# Zipline + Catalyst + Alphalens + PyFolio
# Tier 0 Institutional-Grade Trading System

helmCharts:
  - name: jupyterhub
    repo: https://jupyterhub.github.io/helm-chart
    version: 3.3.7
    releaseName: quantlab-jupyter
    namespace: quantlab
    valuesFile: jupyter-values.yaml

resources:
- namespace.yaml
- configmap.yaml
- pvc.yaml
- service.yaml

labels:
  - pairs:
      app.kubernetes.io/name: quantlab
      app.kubernetes.io/component: research
      tier: "0"