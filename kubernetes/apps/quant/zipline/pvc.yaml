apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zipline-data
  namespace: zipline
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 50Gi