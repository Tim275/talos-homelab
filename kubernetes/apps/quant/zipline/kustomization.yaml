apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 20: Core Applications (Zipline Trading Engine)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "20"

# Tier 0 Multi-Strategy Trading Engine
# Statistical Arbitrage + Momentum + Mean Reversion

resources:
- namespace.yaml
- configmap.yaml
- deployment.yaml
- service.yaml
- pvc.yaml

labels:
  - pairs:
      app.kubernetes.io/name: zipline
      app.kubernetes.io/component: trading-engine
      tier: "0"