apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 20: Core Applications (Strategy Manager)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "20"

# Portfolio Strategy Manager
# Capital Scaling: 100€ -> 50k€

resources:
- namespace.yaml
- configmap.yaml
- deployment.yaml
- service.yaml

labels:
  - pairs:
      app.kubernetes.io/name: strategy-manager
      app.kubernetes.io/component: portfolio-management
      tier: "0"