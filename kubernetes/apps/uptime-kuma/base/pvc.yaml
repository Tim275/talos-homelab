apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptime-kuma-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block-enterprise-retain
  resources:
    requests:
      storage: 2Gi
