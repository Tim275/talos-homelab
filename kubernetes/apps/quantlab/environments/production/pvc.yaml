apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quantlab-research-data
  namespace: quantlab
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 50Gi
---
apiVersion: v1  
kind: PersistentVolumeClaim
metadata:
  name: quantlab-market-data
  namespace: quantlab
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi