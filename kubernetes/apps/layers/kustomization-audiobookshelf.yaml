apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🎵 GRANULAR CONTROL: Audiobookshelf Only
namespace: argocd

resources:
  - audiobookshelf.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: app-audiobookshelf
  enterprise.granular: audiobookshelf-only