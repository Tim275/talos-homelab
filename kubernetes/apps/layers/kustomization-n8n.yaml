apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🔄 GRANULAR CONTROL: N8N Only
namespace: argocd

resources:
  - n8n.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: app-n8n
  enterprise.granular: n8n-only