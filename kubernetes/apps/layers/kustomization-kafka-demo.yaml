apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 📨 GRANULAR CONTROL: Kafka Demo Only
namespace: argocd

resources:
  - kafka-demo.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: app-kafka-demo
  enterprise.granular: kafka-demo-only