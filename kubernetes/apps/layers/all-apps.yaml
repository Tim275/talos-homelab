apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🚀 GRANULAR CONTROL: All Applications Together
namespace: argocd

resources:
  - audiobookshelf.yaml
  - n8n.yaml
  - kafka-demo.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: all-applications
  enterprise.granular: all-apps