# 🚀 APPLICATION SERVICES CONTROL
# ================================
# Ein/Ausschalten: enabled: true/false
# Super einfach Apps kontrollieren!

applications:
  # 🎵 AUDIOBOOKSHELF
  audiobookshelf:
    dev:
      enabled: true
      description: "Audiobook & Podcast Server - Development"
      features:
        - web-interface
        - api-access
        - postgres-db

    prod:
      enabled: true
      description: "Audiobook & Podcast Server - Production"
      features:
        - high-availability
        - backup-enabled
        - monitoring

  # 🔧 N8N AUTOMATION
  n8n:
    dev:
      enabled: true
      description: "Workflow Automation - Development"
      features:
        - workflow-editor
        - webhook-support
        - ai-nodes

    prod:
      enabled: true
      description: "Workflow Automation - Production"
      features:
        - persistent-workflows
        - secure-credentials
        - monitoring

  # ☕ KAFKA DEMO
  kafka-demo:
    dev:
      enabled: true
      description: "Kafka Demo Application"
      features:
        - producer-consumer
        - streaming-demo

  # 📊 QUANTLAB
  quantlab:
    dev:
      enabled: false  # Ausgeschaltet
      description: "Quantitative Lab - Development"
      features:
        - jupyter-notebooks
        - data-analysis

  # 🏠 HOMEASSISTANT
  homeassistant:
    dev:
      enabled: false  # Ausgeschaltet
      description: "Home Automation"
      features:
        - iot-control
        - automations

# USAGE EXAMPLES:
# ==============
# n8n Production ausschalten:
#   n8n:
#     prod:
#       enabled: false
#
# Alle Dev Environments ausschalten:
#   audiobookshelf:
#     dev:
#       enabled: false
#   n8n:
#     dev:
#       enabled: false