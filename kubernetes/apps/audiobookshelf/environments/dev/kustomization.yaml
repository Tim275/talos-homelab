apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 25: Development Applications
commonAnnotations:
  argocd.argoproj.io/sync-wave: "25"

namespace: audiobookshelf-dev

# 🧪 Development Audiobookshelf - Minimal Resources for Testing

helmCharts:
  - name: audiobookshelf
    repo: https://bjw-s.github.io/helm-charts
    version: 1.5.1
    releaseName: audiobookshelf
    namespace: audiobookshelf-dev
    valuesFile: values-dev.yaml

resources:
- namespace.yaml
- pvc-dev.yaml

commonLabels:
  app.kubernetes.io/name: audiobookshelf
  environment: development
  team: timour

# Optional Enterprise Labels (commented out)
# labels:
#   - pairs:
#       service.timourhomelab.dev/tier: "tier-3"
#       service.timourhomelab.dev/environment: "development"