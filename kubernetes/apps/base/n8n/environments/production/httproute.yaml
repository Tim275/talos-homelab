apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: n8n-prod
  namespace: n8n-prod
spec:
  parentRefs:
    - name: timourhomelab-gateway
      namespace: gateway
  hostnames:
    - "n8n.timourhomelab.org"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/"
      backendRefs:
        - name: n8n
          port: 5678
          weight: 100
