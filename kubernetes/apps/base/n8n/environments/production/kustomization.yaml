apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: n8n-prod

resources:
  # - namespace.yaml     # ❌ REMOVED - Platform creates namespace via CNPG
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - pvc.yaml

commonLabels:
  app.kubernetes.io/name: n8n
  environment: production
  team: timour
