apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: n8n-prod

resources:
- namespace.yaml
- resource-quota.yaml
- secret.yaml
# Standard Deployment (commented out for Rollout)
# - deployment.yaml
# - service.yaml
# Progressive Delivery with Argo Rollouts
- rollout.yaml
- canary-services.yaml
- analysis-template.yaml
- configmap.yaml
- pvc.yaml

commonLabels:
  app.kubernetes.io/name: n8n
  environment: production
  team: timour