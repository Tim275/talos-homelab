apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: n8n-dev

resources:
  # - namespace.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - pvc.yaml
  - certificate.yaml
  - httproute.yaml
  # - servicemonitor.yaml #

commonLabels:
  app.kubernetes.io/name: n8n
  environment: development
  team: timour
