apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 25: Production Applications
commonAnnotations:
  argocd.argoproj.io/sync-wave: "25"

namespace: audiobookshelf-prod

# 🏭 Production Audiobookshelf - Enterprise Media Platform

helmCharts:
  - name: audiobookshelf
    repo: https://bjw-s.github.io/helm-charts
    version: 1.5.1
    releaseName: audiobookshelf
    namespace: audiobookshelf-prod
    valuesFile: values.yaml

resources:
- namespace.yaml
- pvc.yaml
- backup.yaml

commonLabels:
  app.kubernetes.io/name: audiobookshelf
  environment: production
  team: timour

# Optional Enterprise Labels (commented out)
# labels:
#   - pairs:
#       service.timourhomelab.dev/tier: "tier-3"
#       service.timourhomelab.dev/environment: "production"