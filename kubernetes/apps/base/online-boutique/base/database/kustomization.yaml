apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Redis Operator CR (NEW - replaces redis.yaml)
  - redis-operator.yaml

commonLabels:
  app.kubernetes.io/managed-by: redis-operator
