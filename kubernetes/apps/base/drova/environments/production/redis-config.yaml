apiVersion: v1
kind: ConfigMap
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  name: redis-drova-external-config
  namespace: drova
data:
  redis.conf: |
    appendonly no
    save 900 1
    save 300 10
    save 60 10000
