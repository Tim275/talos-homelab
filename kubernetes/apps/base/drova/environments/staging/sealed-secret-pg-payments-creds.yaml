# PLACEHOLDER — wird gesealat wenn das Staging-Cluster bereit ist
# kubeseal --cert <staging-cluster-cert> --format yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: REPLACE
  namespace: drova-staging
spec:
  encryptedData: {}
  template:
    metadata:
      name: REPLACE
      namespace: drova-staging
