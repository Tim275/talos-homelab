apiVersion: v1
kind: Secret
metadata:
  name: smtp-config
  namespace: kafka-demo
type: Opaque
stringData:
  # Gmail SMTP Configuration - Consumer uses this to SEND emails
  smtp_host: "smtp.gmail.com"
  smtp_port: "587"
  smtp_username: "timour.miagol@gmail.com"
  smtp_password: "bsds bdjt bulf jamc"  # Gmail App Password
  smtp_from: "timour.miagol@gmail.com"

  # EMAIL FLOW CONFIGURATION:
  # 1. Producer sends Kafka message with ANY recipient email address
  # 2. Consumer receives message and sends email FROM Gmail TO the specified recipient
  # 3. SENDER (always): timour.miagol@gmail.com
  # 4. RECIPIENT (dynamic): Whatever email is in the Kafka message