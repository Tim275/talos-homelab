apiVersion: v1
kind: Secret
metadata:
  name: smtp-config
  namespace: kafka-demo
type: Opaque
stringData:
  # Outlook SMTP Configuration for testing
  smtp_host: "smtp-mail.outlook.com"
  smtp_port: "587"
  smtp_username: "sagichdirnicht259@outlook.de"
  smtp_password: "your-outlook-password"  # You need to replace this with your actual password
  smtp_from: "sagichdirnicht259@outlook.de"

  # Alternative: Gmail SMTP Configuration (commented out)
  # smtp_host: "smtp.gmail.com"
  # smtp_port: "587"
  # smtp_username: "your-email@gmail.com"
  # smtp_password: "your-app-password"
  # smtp_from: "your-email@gmail.com"