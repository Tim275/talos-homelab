apiVersion: v1
kind: Namespace
metadata:
  name: kafka-demo-dev
  labels:
    app.kubernetes.io/name: kafka-demo
    environment: development
    team: timour
    layer: apps  # Triggers Kyverno auto-inject Istio policy
