apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-config
  namespace: kafka-saga
data:
  SPRING_KAFKA_BOOTSTRAP_SERVERS: "my-cluster-kafka-bootstrap.kafka.svc:9092"
  # Apicurio Registry (Confluent-kompatible API auf Port 8081)
  SPRING_KAFKA_PROPERTIES_SCHEMA_REGISTRY_URL: "http://schema-registry.kafka.svc:8081/apis/ccompat/v7"
