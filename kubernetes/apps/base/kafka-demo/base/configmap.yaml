apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-config
  namespace: kafka-saga
data:
  SPRING_KAFKA_BOOTSTRAP_SERVERS: "my-cluster-kafka-bootstrap.kafka.svc:9092"
  # Apicurio Registry - Native API für Avro Serde
  APICURIO_REGISTRY_URL: "http://apicurio-registry.kafka.svc:8080/apis/registry/v2"
