apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-config
  namespace: kafka-saga
data:
  # Kafka Bootstrap Servers
  SPRING_KAFKA_BOOTSTRAP_SERVERS: "my-cluster-kafka-bootstrap.kafka.svc:9092"
  KAFKA_BOOTSTRAP_SERVERS: "my-cluster-kafka-bootstrap.kafka.svc:9092"
  # Apicurio Registry - Native API v3 für Avro Serde 3.x
  APICURIO_REGISTRY_URL: "http://apicurio-registry.kafka.svc:8080/apis/registry/v3"
  SCHEMA_REGISTRY_URL: "http://apicurio-registry.kafka.svc:8080/apis/registry/v3"
