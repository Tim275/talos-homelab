apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: audiobookshelf-dev
  namespace: argocd
  labels:
    app: audiobookshelf
    environment: dev
    team: timour
spec:
  project: app-of-apps
  source:
    repoURL: https://github.com/Tim275/talos-homelab
    targetRevision: HEAD
    path: kubernetes/apps/audiobookshelf/environments/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: audiobookshelf-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true