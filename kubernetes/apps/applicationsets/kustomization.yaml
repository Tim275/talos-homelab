apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 20: Core Applications (ArgoCD ApplicationSets)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "20"

namespace: argocd

resources:
  - apps-dev.yaml
  - apps-production.yaml
  # - namespaces.yaml  # REMOVED - was broken application

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: applications