apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# APPS PROD ENVIRONMENT - COMPONENT CONTROL

resources:
  # RE-ENABLED 2026-05-14 — initContainer + PolicyException fix the chown bug.
  # UptimeKuma serves: (a) Watchdog DMS receiver für Alertmanager,
  # (b) external synthetic pings für drova/grafana/argo/iam endpoints.
  - uptime-kuma/application.yaml
  # - n8n/application.yaml   # MIGRATED 2026-05-09 → applicationsets/tenants/n8n-tenant.yaml
  # - drova/application.yaml   # MIGRATED 2026-05-09 → applicationsets/tenants/drova-tenant.yaml (drova-prod)

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      app.kubernetes.io/part-of: timour-homelab
      app.kubernetes.io/domain: environments
      app.kubernetes.io/layer: apps
      app.kubernetes.io/environment: prod
      team: timour

commonAnnotations:
  argocd.argoproj.io/sync-wave: "26"

