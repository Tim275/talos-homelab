# NOT DEPLOYED — Staging-Cluster kommt separat
# Wenn bereit: Secrets neu sealen + in staging-app.yaml einkommentieren
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: drova-staging

resources:
  - ../../../base/drova/environments/staging

labels:
  - pairs:
      app.kubernetes.io/environment: staging
      app.kubernetes.io/managed-by: argocd
      team: timour
