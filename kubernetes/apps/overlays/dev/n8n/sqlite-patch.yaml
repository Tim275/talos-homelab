# BACKUP: SQLite patch for n8n (if PostgreSQL fails)
# Uncomment this patch and apply it if CloudNative-PG continues to crash
# 
# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: n8n-configmap
# data:
#   # EMERGENCY: Use SQLite instead of PostgreSQL (CloudNative-PG is crashing)
#   DB_TYPE: "sqlite"
#   DB_SQLITE_DATABASE: "/home/node/.n8n/database.sqlite"
#   # Comment out PostgreSQL config to avoid conflicts
#   # DB_POSTGRESDB_HOST: "n8n-postgres-rw.n8n-dev.svc.cluster.local"