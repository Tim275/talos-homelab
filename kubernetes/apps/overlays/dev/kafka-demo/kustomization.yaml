apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kafka-demo

resources:
- ../../../base/kafka-demo

patchesStrategicMerge:
- producer-dev-patch.yaml
- consumer-dev-patch.yaml

namePrefix: dev-

commonLabels:
  environment: dev
  app.kubernetes.io/instance: kafka-demo-dev