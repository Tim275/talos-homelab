apiVersion: apps/v1
kind: Deployment
metadata:
  name: n8n
  annotations:
    backup.velero.io/backup-volumes: data
spec:
  template:
    metadata:
      annotations:
        backup.velero.io/backup-volumes: data