apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: audiobookshelf
  namespace: audiobookshelf
spec:
  storageClassName: rook-ceph-block-enterprise
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi