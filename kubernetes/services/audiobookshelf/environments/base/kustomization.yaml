apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 20: Core Applications (Base AudioBookshelf)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "20"

resources:
- namespace.yaml
- pvc.yaml
- deployment.yaml
- service.yaml

patches:
  - path: stateful-deployment-patch.yaml
    target:
      kind: Deployment
      name: audiobookshelf