# Controllers Stack — 1 ApplicationSet ersetzt 5 separate Application-CRs
# Includes: argocd (self-manage), argo-rollouts, cert-manager, operators, sealed-secrets
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: controllers-stack
  namespace: argocd
  labels:
    app.kubernetes.io/managed-by: argocd
    infrastructure.domain: controllers
    team: timour
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - matrix:
        generators:
          - clusters:
              selector:
                matchLabels:
                  argocd.argoproj.io/secret-type: cluster   # all clusters
          - list:
              elements:
                - { component: sealed-secrets,  appName: sealed-secrets,  path: kubernetes/infrastructure/controllers/sealed-secrets/overlays/prod,  namespace: sealed-secrets,  wave: "-3" }
                - { component: cert-manager,    appName: cert-manager,    path: kubernetes/infrastructure/controllers/cert-manager/overlays/prod,    namespace: cert-manager,    wave: "-2" }
                - { component: operators,       appName: operators,       path: kubernetes/infrastructure/controllers/operators/overlays/prod,       namespace: argocd,          wave: "-1" }
                - { component: argo-rollouts,   appName: argo-rollouts,   path: kubernetes/infrastructure/controllers/argo-rollouts/overlays/prod,   namespace: argo-rollouts,   wave: "1" }
                - { component: argocd,          appName: argocd,          path: kubernetes/infrastructure/controllers/argocd/overlays/prod,          namespace: argocd,          wave: "1" }
  template:
    metadata:
      name: '{{.appName}}'
      labels:
        app.kubernetes.io/managed-by: argocd
        infrastructure.domain: controllers
        infrastructure.component: '{{.component}}'
        team: timour
      annotations:
        argocd.argoproj.io/sync-wave: '{{.wave}}'
    spec:
      project: infrastructure
      source:
        repoURL: https://github.com/Tim275/talos-homelab
        targetRevision: HEAD
        path: '{{.path}}'
      destination:
        server: '{{.server}}'
        namespace: '{{.namespace}}'
      ignoreDifferences:
        - group: bitnami.com
          kind: SealedSecret
          jsonPointers:
            - /metadata/annotations
            - /metadata/labels
      syncPolicy:
        automated: { prune: true, selfHeal: true }
        syncOptions:
          - CreateNamespace=true
          - PrunePropagationPolicy=foreground
          - PruneLast=true
          - RespectIgnoreDifferences=true
          - ServerSideApply=true
        retry:
          limit: 5
          backoff: { duration: 5s, factor: 2, maxDuration: 3m }
