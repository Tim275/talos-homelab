# Observability Stack — 1 ApplicationSet ersetzt 15 separate Application-CRs
#
# Selector: clusters mit Label observability.tier=enabled
# Multi-Cluster-Ready: Pi-Spoke registrieren mit Label → 15 Apps auto-generated
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: observability-stack
  namespace: argocd
  labels:
    app.kubernetes.io/managed-by: argocd
    infrastructure.domain: observability
    team: timour
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - matrix:
        generators:
          - clusters:
              selector:
                matchLabels:
                  observability.tier: enabled
                  environment: prod
          - list:
              elements:
                # METRICS
                - { component: prometheus,             appName: kube-prometheus-stack-in-cluster, path: kubernetes/40-observability-crds/kube-prometheus-stack/overlays/prod, namespace: monitoring,   wave: "40" }
                - { component: metrics-server,         appName: metrics-server,                   path: kubernetes/50-observability-stack/metrics-server/overlays/prod,         namespace: kube-system,  wave: "50" }
                - { component: blackbox-exporter,      appName: blackbox-exporter,                path: kubernetes/50-observability-stack/blackbox-exporter/overlays/prod,      namespace: monitoring,   wave: "50" }
                - { component: pve-exporter,           appName: pve-exporter,                     path: kubernetes/50-observability-stack/pve-exporter/overlays/prod,           namespace: monitoring,   wave: "50" }
                # LOGS
                - { component: elasticsearch,          appName: elasticsearch,                    path: kubernetes/50-observability-stack/elasticsearch/overlays/prod,             namespace: elastic-system, wave: "50" }
                - { component: loki,                   appName: loki,                             path: kubernetes/50-observability-stack/loki/overlays/prod,                     namespace: monitoring,   wave: "50" }
                - { component: vector,                 appName: vector,                           path: kubernetes/50-observability-stack/vector/overlays/prod,                   namespace: monitoring,   wave: "50" }
                - { component: kibana,                 appName: kibana,                           path: kubernetes/50-observability-stack/kibana/overlays/prod,                   namespace: elastic-system, wave: "50" }
                # TRACES
                - { component: tempo,                  appName: tempo,                            path: kubernetes/50-observability-stack/tempo/overlays/prod,                  namespace: monitoring,   wave: "50" }
                - { component: jaeger,                 appName: jaeger,                           path: kubernetes/50-observability-stack/jaeger/overlays/prod,                 namespace: jaeger,       wave: "50" }
                - { component: opentelemetry,          appName: opentelemetry,                    path: kubernetes/50-observability-stack/opentelemetry/overlays/prod,          namespace: opentelemetry, wave: "6" }
                # DASHBOARDS / UI
                - { component: grafana,                appName: grafana,                          path: kubernetes/99-monitoring-config/dashboards/grafana/overlays/prod,            namespace: grafana,      wave: "99" }
                - { component: dashboards,             appName: dashboards,                       path: kubernetes/99-monitoring-config/dashboards/configs/overlays/prod,            namespace: grafana,      wave: "9" }
                - { component: hubble,                 appName: hubble,                           path: kubernetes/99-monitoring-config/dashboards/hubble/overlays/prod,             namespace: kube-system,  wave: "9" }
                # ALERTING
                - { component: infrastructure-alerts,  appName: infrastructure-alerts,            path: kubernetes/99-monitoring-config/alerting/alerts/overlays/prod,               namespace: monitoring,   wave: "9" }
  template:
    metadata:
      name: '{{.appName}}'
      labels:
        app.kubernetes.io/managed-by: argocd
        infrastructure.domain: observability
        infrastructure.component: '{{.component}}'
        team: timour
      annotations:
        argocd.argoproj.io/sync-wave: '{{.wave}}'
    spec:
      project: infrastructure
      source:
        repoURL: https://github.com/Tim275/talos-homelab
        targetRevision: HEAD
        path: '{{.path}}'
      destination:
        server: '{{.server}}'
        namespace: '{{.namespace}}'
      ignoreDifferences:
        - group: bitnami.com
          kind: SealedSecret
          jsonPointers:
            - /metadata/annotations
            - /metadata/labels
        - group: monitoring.coreos.com
          kind: ServiceMonitor
          jsonPointers:
            - /metadata/annotations
            - /metadata/labels
      syncPolicy:
        automated: { prune: true, selfHeal: true }
        syncOptions:
          - CreateNamespace=true
          - PrunePropagationPolicy=foreground
          - PruneLast=true
          - RespectIgnoreDifferences=true
          - ServerSideApply=true
        retry:
          limit: 5
          backoff: { duration: 5s, factor: 2, maxDuration: 3m }
