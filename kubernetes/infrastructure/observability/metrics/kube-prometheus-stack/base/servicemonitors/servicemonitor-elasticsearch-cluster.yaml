apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: elasticsearch-cluster-exporter
  namespace: elastic-system
  annotations:
    argocd.argoproj.io/tracking-id: kube-prometheus-stack:monitoring.coreos.com/ServiceMonitor:monitoring/elasticsearch-cluster-exporter
  labels:
    argocd.argoproj.io/instance: kube-prometheus-stack
    app: elasticsearch-exporter
    release: prometheus-operator
    vm-scrape: enabled
    argocd.argoproj.io/sync-wave: "3"
spec:
  selector:
    matchLabels:
      app: elasticsearch-exporter
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
      honorLabels: true
  namespaceSelector:
    matchNames:
      - elastic-system
