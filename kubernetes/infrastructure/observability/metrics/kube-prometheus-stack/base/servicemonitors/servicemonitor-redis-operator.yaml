apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: redis-operator
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - redis-operator-system
  selector:
    matchLabels:
      name: redis-operator
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
