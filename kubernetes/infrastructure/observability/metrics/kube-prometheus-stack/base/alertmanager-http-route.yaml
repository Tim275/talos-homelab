# ENTERPRISE: Alertmanager HTTPRoute with correct gateway and domain
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: alertmanager
  namespace: monitoring
spec:
  parentRefs:
    - name: envoy-gateway  # Fixed: Changed from "internal" to "envoy-gateway"
      namespace: gateway
  hostnames:
    - alertmanager.timourhomelab.org  # Fixed: Changed from stonegarden.dev to timourhomelab.org
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: alertmanager-operated
          port: 9093
