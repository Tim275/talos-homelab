apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: observability-

# 🌊 Sync Wave 5: Log Visualization (Observability Stack)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

# resources:
  # - kibana-httproute.yaml  # Disabled for now

labels:
  - pairs:
      app.kubernetes.io/part-of: observability-stack
      app.kubernetes.io/component: log-visualization