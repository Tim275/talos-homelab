apiVersion: v1
kind: Service
metadata:
  name: vmagent-direct
  namespace: monitoring
  labels:
    app: vmagent-direct
spec:
  ports:
    - port: 8429
      name: http
  selector:
    app: vmagent-direct
