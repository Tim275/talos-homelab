apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: compliance-trivy
  labels:
    release: kube-prometheus-stack
spec:
  groups:
    # ─────────────────────────────────────────────────────────────────────
    # TRIVY — CVE detection (trivy-operator deployed, no alerts on findings)
    # ─────────────────────────────────────────────────────────────────────
    - name: trivy
      interval: 5m
      rules:
        # DORMANT until trivy-operator is deployed (task #23): trivy_image_vulnerabilities
        # only exists once the operator runs. Harmless (absent metric never fires) — activates
        # automatically when #23 lands. Don't mistake "no CVE alerts" for "no CVEs" until then.
        - alert: TrivyCriticalCVEFound
          expr: |
            sum by (namespace) (trivy_image_vulnerabilities{severity="Critical"}) > 0
          for: 1h
          labels:
            severity: warning
            priority: P2
            component: trivy
          annotations:
            dashboard_url: "/d/kyverno"
            summary: "Critical CVE in {{ $labels.namespace }} ({{ $value }} findings)"
            description: "Critical-severity vulnerability detected — patch image or accept exception."
            action: "kubectl get vulnerabilityreports -n {{ $labels.namespace }} -o wide"
