apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 5: Elasticsearch Cluster (AFTER ECK Operator Wave 1)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

commonLabels:
  app.kubernetes.io/name: elasticsearch
  app.kubernetes.io/component: log-storage
  app.kubernetes.io/part-of: observability-stack

resources:
  - elasticsearch-cluster.yaml
  - kibana.yaml
  - elasticsearch-exporter-deployment.yaml
  - elasticsearch-exporter-service.yaml
  - elasticsearch-servicemonitor.yaml
  - elasticsearch-ilm-policies.yaml
  - enterprise-index-templates.yaml
  - enterprise-ilm-policies.yaml
