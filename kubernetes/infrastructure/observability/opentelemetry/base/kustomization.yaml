apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: opentelemetry

resources:
  - rbac.yaml
  - collector.yaml      # Agent-Tier (DaemonSet)
  - gateway.yaml        # Gateway-Tier (Deployment, tail_sampling)
