# OpenTelemetry Operator Configuration for Homelab
# Minimal configuration - using chart defaults

# Required collector image configuration
manager:
  collectorImage:
    repository: "otel/opentelemetry-collector-contrib"

# Auto-instrumentation support
admissionWebhooks:
  certManager:
    enabled: true