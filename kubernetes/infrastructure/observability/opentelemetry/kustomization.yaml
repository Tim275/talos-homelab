apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


# 🌊 Sync Wave 5: OpenTelemetry Observability (After Storage)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

namespace: opentelemetry

resources:
  - ns.yaml
  - rbac.yaml                          # 🔐 RBAC for kubeletstats access
  - collector.yaml
  # - telemetry-config.yaml          # ❌ DISABLED: Istio Telemetry must stay in istio-system namespace

# ✅ OpenTelemetry Operator moved to controllers/operators/opentelemetry-operator
# helmCharts section removed - operator now deployed separately at wave 0
