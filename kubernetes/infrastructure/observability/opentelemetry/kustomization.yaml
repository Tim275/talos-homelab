apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


# 🌊 Sync Wave 5: OpenTelemetry Observability (After Storage)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

namespace: opentelemetry

resources:
  - ns.yaml
  - collector.yaml
  # - telemetry-config.yaml          # ❌ DISABLED: Istio Telemetry must stay in istio-system namespace

helmCharts:
  - name: opentelemetry-operator
    repo: https://open-telemetry.github.io/opentelemetry-helm-charts
    version: "0.95.1"
    valuesFile: values.yaml
    namespace: opentelemetry
    includeCRDs: true
