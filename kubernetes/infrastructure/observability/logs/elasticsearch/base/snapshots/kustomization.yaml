apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - bucket.yaml
  - create-s3-credentials-job.yaml
  - register-repository-job.yaml
  - slm-policy-job.yaml

patchesStrategicMerge:
  - job-resource-limits-patch.yaml
