apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

resources:
  # promtail.yaml DEACTIVATED 2026-05-18 — Vector DaemonSet+Aggregator handelt
  # bereits ALL Pod-Logs zu Loki+ES. Doppel-Ingest halbiert Loki-CPU+Storage.
  # Re-aktivieren wenn Vector ausfällt.
  # - promtail.yaml
  - poddisruptionbudget.yaml
  - loki-logs-bucket.yaml

helmCharts:
  - name: loki
    repo: https://grafana.github.io/helm-charts
    version: "6.10.2"
    releaseName: loki
    namespace: monitoring
    valuesFile: values.yaml
