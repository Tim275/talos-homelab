apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: observability-elasticsearch
  namespace: argocd
  labels:
    infrastructure.layer: observability
    enterprise.granular: elasticsearch-only
    team: timour
spec:
  goTemplate: true
  generators:
    # 🔍 GRANULAR CONTROL: Elasticsearch Only
    - list:
        elements:
          - name: elasticsearch
            path: kubernetes/infrastructure/observability/elasticsearch
            namespace: elastic-system
            critical: "true"
            component: "search-analytics"
            wave: "5"
  template:
    metadata:
      name: 'observability-{{.name}}'
      labels:
        infrastructure.layer: observability
        infrastructure.component: '{{.component}}'
        infrastructure.critical: '{{.critical}}'
        app.kubernetes.io/managed-by: argocd
        enterprise.granular: elasticsearch-only
        team: timour
      annotations:
        argocd.argoproj.io/sync-wave: '{{.wave}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/Tim275/talos-homelab
        targetRevision: HEAD
        path: '{{.path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{.namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true