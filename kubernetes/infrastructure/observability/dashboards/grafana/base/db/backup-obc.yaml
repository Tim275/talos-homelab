apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: cnpg-grafana-backup
  namespace: grafana
  annotations:
    argocd.argoproj.io/sync-wave: "-4"
spec:
  bucketName: cnpg-grafana-backup
  storageClassName: rook-ceph-bucket
