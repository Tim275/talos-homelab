apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-drova-datasources-provisioning
  namespace: grafana
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/component: datasource
data:
  drova-datasources.yaml: |
    apiVersion: 1
    datasources:
      - name: Postgres Drova Trips
        type: postgres
        access: proxy
        url: drova-postgres-ro.drova.svc.cluster.local:5432
        user: grafana_reader
        isDefault: false
        database: drova_trips
        uid: postgres-drova-trips
        jsonData:
          database: drova_trips
          sslmode: prefer
          postgresVersion: 1601
          timescaledb: false
        secureJsonData:
          password: $__env{DROVA_POSTGRES_GRAFANA_READER_PASSWORD}
      - name: Postgres Drova Payments
        type: postgres
        access: proxy
        url: drova-postgres-ro.drova.svc.cluster.local:5432
        user: grafana_reader
        isDefault: false
        database: drova_payments
        uid: postgres-drova-payments
        jsonData:
          database: drova_payments
          sslmode: prefer
          postgresVersion: 1601
          timescaledb: false
        secureJsonData:
          password: $__env{DROVA_POSTGRES_GRAFANA_READER_PASSWORD}
      - name: Postgres Drova Drivers
        type: postgres
        access: proxy
        url: drova-postgres-ro.drova.svc.cluster.local:5432
        user: grafana_reader
        isDefault: false
        database: drova_drivers
        uid: postgres-drova-drivers
        jsonData:
          database: drova_drivers
          sslmode: prefer
          postgresVersion: 1601
          timescaledb: false
        secureJsonData:
          password: $__env{DROVA_POSTGRES_GRAFANA_READER_PASSWORD}
      - name: Postgres Drova Users
        type: postgres
        access: proxy
        url: drova-postgres-ro.drova.svc.cluster.local:5432
        user: grafana_reader
        isDefault: false
        database: drova_users
        uid: postgres-drova-users
        jsonData:
          database: drova_users
          sslmode: prefer
          postgresVersion: 1601
          timescaledb: false
        secureJsonData:
          password: $__env{DROVA_POSTGRES_GRAFANA_READER_PASSWORD}
      - name: Elasticsearch
        type: elasticsearch
        access: proxy
        url: https://production-cluster-es-http.elastic-system.svc.cluster.local:9200
        isDefault: false
        uid: elasticsearch
        basicAuth: true
        basicAuthUser: grafana_reader
        jsonData:
          timeField: "@timestamp"
          esVersion: "8.0.0"
          logMessageField: message
          logLevelField: level
          tlsSkipVerify: true
          index: "logs-*"
          interval: Daily
          timeInterval: "30s"
          maxConcurrentShardRequests: 5
        secureJsonData:
          basicAuthPassword: $__env{ES_GRAFANA_READER_PASSWORD}
