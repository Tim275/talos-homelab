apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

commonAnnotations:
  argocd.argoproj.io/sync-wave: "7"

resources:
  # DEPRECATED: ConfigMap-based dashboards removed
  # NEW: Using Grafana Operator CRDs in grafana/enterprise-dashboards/
  # Nothing to deploy here - dashboards managed by Grafana Operator!

labels:
  - pairs:
      app.kubernetes.io/part-of: monitoring-stack
      app.kubernetes.io/component: monitoring-dashboards
