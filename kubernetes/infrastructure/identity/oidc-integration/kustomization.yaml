apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🔐 OIDC Integration Layer
# Infrastructure configuration for kube-apiserver OIDC authentication

commonAnnotations:
  argocd.argoproj.io/sync-wave: "1"

# NOTE: Talos machine config patches are NOT deployed via Kustomize
# They must be applied manually via talosctl:
#
#   talosctl patch mc --nodes <control-plane> \
#     --patch @talos-patches/apiserver-oidc.yaml
#
# This kustomization.yaml exists for documentation and future
# Kubernetes-native OIDC resources (e.g., OIDCClients CRD)

resources: []

# Future: When using OIDC Operator or CRDs
# resources:
#   - oidc-provider.yaml
#   - oidc-client.yaml
