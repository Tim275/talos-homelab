apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: coturn-tls
  namespace: coturn
spec:
  secretName: coturn-tls
  dnsNames:
  - coturn.timourhomelab.org
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
