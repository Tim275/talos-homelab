apiVersion: v1
kind: Service
metadata:
  name: signal
  namespace: netbird
  labels:
    app.kubernetes.io/name: signal
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: signal
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
