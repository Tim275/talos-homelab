apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: agent-setup-key
  namespace: netbird
spec:
  encryptedData:
    NB_SETUP_KEY: PLACEHOLDER_SEAL_WITH_KUBESEAL
  template:
    metadata:
      name: agent-setup-key
      namespace: netbird
