apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: management
  namespace: netbird
spec:
  encryptedData:
    datastoreEncryptionKey: PLACEHOLDER_SEAL_WITH_KUBESEAL
  template:
    metadata:
      name: management
      namespace: netbird
