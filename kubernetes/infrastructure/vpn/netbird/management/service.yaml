apiVersion: v1
kind: Service
metadata:
  name: management
  namespace: netbird
  labels:
    app.kubernetes.io/name: management
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: management
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
