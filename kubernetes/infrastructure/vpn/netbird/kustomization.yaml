apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: netbird

resources:
  # Namespace (Wave 0)
  - ns.yaml

  # Sealed Secrets (Wave 2)
  - coturn-credentials-sealed.yaml
  - authelia-oidc-credentials-sealed.yaml
  - relay-secret-sealed.yaml

  # Netbird Components (Wave 3)
  - management/
  - signal/
  - dashboard/
  - relay/

  # Gateway Routes (Wave 4)
  - httproute.yaml
  - grpcroute.yaml
