apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app.kubernetes.io/managed-by: argocd
  infrastructure.domain: vpn

commonAnnotations:
  argocd.argoproj.io/sync-wave: "3"

resources:
  # Tailscale 2026-05-08 disabled
  # - tailscale/application.yaml
  # NetBird 2026-05-11 disabled — Geo-DB-Init-Bug (kein workaround mit CF Free),
  # plus CF Free strippt gRPC trailers. Parked bis Vodafone Static-IP ODER
  # NetBird Cloud-Variante. CLI von Café funktioniert eh nicht stable.
  # - netbird/application.yaml
