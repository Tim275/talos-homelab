apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Tailscale Enterprise Operator Pattern (Production)
# Using official Kubernetes Operator + Connector CRD
# Managed via Helm + GitOps (ArgoCD)
resources:
  # - tailscale/  # ❌ DEPRECATED: Manual deployment (replaced by operator)
  - tailscale-operator/  # ✅ ACTIVE: Enterprise Operator + Connector CRD
