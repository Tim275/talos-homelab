apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Tailscale GitOps deployment (homelab pattern)
# NOTE: For production/enterprise, Talos System Extension is recommended
# See tofu/talos/machine-config/common.yaml.tftpl for extension config
resources:
  - tailscale/
