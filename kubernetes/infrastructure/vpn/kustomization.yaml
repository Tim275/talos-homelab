apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app.kubernetes.io/managed-by: argocd
  infrastructure.domain: vpn

commonAnnotations:
  argocd.argoproj.io/sync-wave: "3"

resources:
  # Tailscale 2026-05-08 final disabled — Replaced by CF WARP (Zero Trust).
  # CF WARP gibt Layer-3-Connectivity zu Cluster-IPs ohne extra VPN-Pod im
  # Cluster. Free-Tier 50 Users. Replikation von vehagn pattern.
  # - tailscale/application.yaml
  - netbird/application.yaml
