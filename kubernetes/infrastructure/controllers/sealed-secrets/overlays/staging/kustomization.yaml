# sealed-secrets controller für Staging Edge-Cluster
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sealed-secrets

resources:
  - ../../base

components:
  - ../../../../../components/arm64-arch
  - ../../../../../components/single-replica
