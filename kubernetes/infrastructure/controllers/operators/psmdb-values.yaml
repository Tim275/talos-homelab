# PSMDB Operator values - Cluster-wide mode for multi-tenant MongoDB
# Setting watchAllNamespaces enables operator to watch all namespaces
# This is required for MongoDB deployments in tenant namespaces (e.g., oms)

watchAllNamespaces: true
