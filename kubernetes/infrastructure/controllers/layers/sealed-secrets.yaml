apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🔐 GRANULAR CONTROL: Sealed Secrets Only
namespace: argocd

resources:
  - ../sealed-secrets/kustomization.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: controllers-sealed-secrets
  enterprise.granular: sealed-secrets-only