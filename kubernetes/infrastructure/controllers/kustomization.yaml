apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🎮 CONTROLLER LAYER - TRUE KUSTOMIZE CONTROL!
# ==============================================
# EINFACH KOMMENTIEREN/AUSKOMMENTIEREN für An/Aus!
# Genau wie du es willst - TRUE Enterprise Pattern!

# namespace: argocd                     # ❌ DISABLED - Let applications define their own namespaces

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      infrastructure.layer: controllers

resources:
  - project.yaml

  # 🚀 INDIVIDUAL CONTROLLER APPLICATIONS
  # Comment/Uncomment to enable/disable controllers!

  # Core Controllers (Required)
  # - argocd/application.yaml           # ❌ DISABLED - ArgoCD manually bootstrapped
  - sealed-secrets/application.yaml
  - cert-manager/application.yaml
  - argo-rollouts/application.yaml

  # Optional Controllers (Toggle with #)
  # - cloudnative-pg/application.yaml    # ❌ DISABLED
  # - cluster-autoscaler/application.yaml # ❌ DISABLED
  # - external-secrets/application.yaml   # ❌ DISABLED