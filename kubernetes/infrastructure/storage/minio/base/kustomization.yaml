apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


# Sync Wave 4: MinIO Storage (After Controllers)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "4"

namespace: minio

resources:
  - ns.yaml
  - httproute.yaml

helmCharts:
  - name: minio
    repo: https://charts.min.io/
    version: 5.2.0
    releaseName: minio
    valuesFile: values.yaml
    includeCRDs: true