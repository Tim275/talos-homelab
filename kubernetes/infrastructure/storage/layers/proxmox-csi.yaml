apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 💾 GRANULAR CONTROL: Proxmox CSI Only
namespace: argocd

resources:
  - ../proxmox-csi/kustomization.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: storage-proxmox-csi
  enterprise.granular: proxmox-csi-only