apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 💾 GRANULAR CONTROL: Rook Ceph Only
namespace: argocd

resources:
  - ../rook-ceph/kustomization.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: storage-rook-ceph
  enterprise.granular: rook-ceph-only