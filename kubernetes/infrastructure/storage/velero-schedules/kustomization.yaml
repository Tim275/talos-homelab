apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Enterprise Best Practice: Separate Configuration from Application
# Velero Operator (Helm) + Schedules (Plain Manifests) = Clean Separation

namespace: velero

resources:
  - backup-schedules.yaml
