apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# : Separate Configuration from Application
# Velero Operator (Helm) + Schedules (Plain Manifests) = Clean Separation

namespace: velero

resources:
  - backup-schedules.yaml
  - tier0-databases-6h-schedule.yaml   # MOVED from velero/base/ 2026-05-18 (dedupe)
  - keycloak-schedule.yaml             # MOVED from velero/base/schedules/ 2026-05-18
  - restore-verify-cronjob.yaml        # Auto-Verify Schrödinger's Backup elimination
  - quartal-drill-reminder.yaml        # Slack-Reminder alle 3 Monate für DR-Drill
