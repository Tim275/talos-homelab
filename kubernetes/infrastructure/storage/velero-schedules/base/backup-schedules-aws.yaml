# Deaktiviert — Tier-0 taeglich zusaetzlich zu AWS S3, ~0,50 EUR/Monat. Ceph RGW bleibt primaer (6h).
# apiVersion: velero.io/v1
# kind: Schedule
#   name: tier0-aws-n8n-prod
#   namespace: velero
#     backup-tier: tier-0
#     backup-target: aws-s3-offsite
#     app: n8n-prod
#       - n8n-prod
#     storageLocation: aws-s3-offsite
#     defaultVolumesToFsBackup: true  # Restic client-side encryption
#         backup-tier: tier-0
#         backup-target: aws-s3-offsite
#         namespace: n8n-prod
# apiVersion: velero.io/v1
# kind: Schedule
#   name: tier0-aws-keycloak
#   namespace: velero
#     backup-tier: tier-0
#     backup-target: aws-s3-offsite
#     app: keycloak
#   schedule: "0 4 * * *"
#       - keycloak
#     storageLocation: aws-s3-offsite
#     defaultVolumesToFsBackup: true
#         backup-tier: tier-0
#         backup-target: aws-s3-offsite
#         namespace: keycloak
# apiVersion: velero.io/v1
# kind: Schedule
#   name: tier0-aws-infisical
#   namespace: velero
#     backup-tier: tier-0
#     backup-target: aws-s3-offsite
#     app: infisical
#   schedule: "0 4 * * *"
#       - infisical
#     storageLocation: aws-s3-offsite
#     defaultVolumesToFsBackup: true
#         backup-tier: tier-0
#         backup-target: aws-s3-offsite
#         namespace: infisical
# apiVersion: velero.io/v1
# kind: Schedule
#   name: tier0-aws-lldap
#   namespace: velero
#     backup-tier: tier-0
#     backup-target: aws-s3-offsite
#     app: lldap
#   schedule: "0 4 * * *"
#       - lldap
#     storageLocation: aws-s3-offsite
#     defaultVolumesToFsBackup: true
#         backup-tier: tier-0
#         backup-target: aws-s3-offsite
#         namespace: lldap
