apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 6: Velero UI (After Velero)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "6"

# 🎯 OTWLD Velero UI Application + HTTPRoute
resources:
  - application.yaml  # Helm Application for OTWLD Velero UI
  - ingress.yaml      # Gateway API HTTPRoute
