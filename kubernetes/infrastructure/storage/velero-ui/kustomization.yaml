apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 6: Velero UI (After Velero)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "6"

resources:
  - application.yaml
  # - ingress.yaml  # DISABLED: No authentication - internal use only via kubectl port-forward
