apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: allow-tailscale-httproute
  namespace: rook-ceph
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: tailscale
  to:
    - group: ""
      kind: Service
