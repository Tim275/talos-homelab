existingConfigSecret: proxmox-csi-plugin

livenessprobe:
  image:
    tag: v2.13.1

storageClass:
  - name: proxmox-csi
    cache: writethrough
    fstype: ext4
    reclaimPolicy: Retain
    ssd: true
    storage: local-zfs
    mountOptions:
      - noatime