apiVersion: ceph.rook.io/v1
kind: CephObjectStore
metadata:
  name: homelab-objectstore
  namespace: rook-ceph
spec:
  metadataPool:
    replicated:
      size: 3
  dataPool:
    replicated:
      size: 3
  preservePoolsOnDelete: false
  gateway:
    port: 80
    securePort: 443
    # TLS configuration for HTTPS
    sslCertificateRef: ceph-rgw-tls-secret
    # Homelab: 1 instance sufficient, multiple instances provide HA in production
    instances: 1
    resources:
      limits:
        cpu: "1500m"
        memory: "1.5Gi"  # Reduced from 2Gi to save memory
      requests:
        cpu: "500m"
        memory: "512Mi"
