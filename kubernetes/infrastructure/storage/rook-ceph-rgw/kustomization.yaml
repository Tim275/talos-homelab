apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


# 🌊 Sync Wave 4: Rook-Ceph RGW Storage (After Controllers)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "4"

namespace: rook-ceph

resources:
  - certificate.yaml           # TLS cert for HTTPS (wave 10)
  - ceph-object-store.yaml     # RGW with HTTPS enabled
  - ceph-object-store-user.yaml
