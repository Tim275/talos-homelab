apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


# 🌊 Sync Wave 4: Rook-Ceph RGW Storage (After Controllers)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "4"

namespace: rook-ceph

resources:
  - certificate.yaml           # TLS cert for HTTPS (moved to wave 3)
  - ceph-object-zone.yaml      # Realm/ZoneGroup/Zone (wave 3 - required before ObjectStore)
  - ceph-object-store.yaml     # RGW with HTTPS enabled
  - ceph-object-store-user.yaml
