apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🎯 TRUE KUSTOMIZE CONTROL - COMMENT/UNCOMMENT = ENABLE/DISABLE!
# ===============================================================
# This is what you wanted for 35 hours! Pure kustomize control!

commonLabels:
  app.kubernetes.io/managed-by: argocd
  infrastructure.domain: storage

resources:
  # 🏛️ STORAGE PROJECT (keep this)
  - project.yaml

  # 🎯 STORAGE COMPONENTS - COMMENT/UNCOMMENT TO CONTROL!
  # ====================================================

  # ✅ ENABLED STORAGE COMPONENTS
  # - csi-drivers/            # ❌ DISABLED - Empty kustomization.yaml
  - rook-ceph/               # Distributed storage with Ceph
  - rook-ceph-rgw/           # Object storage gateway
  - proxmox-csi/             # Proxmox CSI driver
  - layers/                  # Storage layer configs

  # ❌ DISABLED STORAGE COMPONENTS (comment out to disable)
  # - minio/                 # ❌ DISABLED - Object storage not needed
  # - longhorn/              # ❌ DISABLED - Alternative storage solution
  # - velero/                # ❌ DISABLED - Backup solution not needed yet