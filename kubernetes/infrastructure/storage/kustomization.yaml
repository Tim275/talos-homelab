apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🎯 TRUE KUSTOMIZE CONTROL - COMMENT/UNCOMMENT = ENABLE/DISABLE!
# ===============================================================
# This is what you wanted for 35 hours! Pure kustomize control!

commonLabels:
  app.kubernetes.io/managed-by: argocd
  infrastructure.domain: storage

resources:
  # 🏛️ STORAGE PROJECT (keep this)
  - project.yaml

  # 🎯 CHILD APPLICATIONS - APPEAR IN ARGOCD UI
  # ===========================================
  - rook-ceph/application.yaml
  - rook-ceph-rgw/application.yaml
  # - proxmox-csi/application.yaml  # ❌ No application.yaml file exists

  # - csi-drivers/   # Empty kustomization
  # - minio/         # Not needed
  # - longhorn/      # Alternative solution
  - velero/application.yaml  # ✅ N8N Backup enabled
