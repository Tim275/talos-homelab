---
# ObjectBucketClaim for Velero backups
apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: velero-backups
  namespace: velero
spec:
  bucketName: velero-backups
  storageClassName: rook-ceph-bucket
  additionalConfig:
    maxObjects: "1000000"
    maxSize: "100G"
