---
# 📦 Velero Cluster Backups - Ceph User
# ======================================
# S3 User for Kubernetes cluster resources backup
# Bucket: velero-cluster-backups
# Contains: ConfigMaps, Secrets, CRDs, Deployments, etc.
apiVersion: ceph.rook.io/v1
kind: CephObjectStoreUser
metadata:
  name: velero-cluster-backups
  namespace: rook-ceph
spec:
  store: homelab-objectstore
  displayName: "Velero Cluster Backups"
  capabilities:
    user: "*"
    bucket: "*"
