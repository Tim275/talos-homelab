---
# Velero PersistentVolume Backups - Ceph User
# ===============================================
# S3 User for PV backups via Restic/Kopia
# Bucket: velero-pv-backups
# Contains: File-based backups of all PVs (encrypted client-side)
apiVersion: ceph.rook.io/v1
kind: CephObjectStoreUser
metadata:
  name: velero-pv-backups
  namespace: rook-ceph
spec:
  store: homelab-objectstore
  displayName: "Velero PV Backups"
  capabilities:
    user: "*"
    bucket: "*"
