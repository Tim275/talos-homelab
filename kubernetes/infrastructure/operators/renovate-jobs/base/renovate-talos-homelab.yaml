apiVersion: renovate-operator.mogenius.com/v1alpha1
kind: RenovateJob
metadata:
  name: renovate-talos-homelab
  namespace: renovate-operator
spec:
  discoveryFilters:
    - Tim275/talos-homelab
  provider:
    name: github
    endpoint: ""  # defaults to https://api.github.com
  image: ghcr.io/renovatebot/renovate:43.176.3   # renovate: docker=ghcr.io/renovatebot/renovate
  parallelism: 1
  schedule: "0 */6 * * *"
  secretRef: renovate-secret
  extraEnv:
    - name: LOG_LEVEL
      value: info
    - name: RENOVATE_GIT_AUTHOR
      value: "Renovate Bot <tim275@users.noreply.github.com>"
  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      # kein CPU-Limit: Batch-Job, nutzt real ~573m und lief in 115k CFS-Throttles/7d.
      # Der 200m-Request deckelt ihn unter Node-Druck ohnehin ueber CPU-Shares.
      memory: 2Gi
