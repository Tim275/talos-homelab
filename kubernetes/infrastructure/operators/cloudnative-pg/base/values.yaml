# defaults 15/10 too tight for our etcd p99 spikes (~4.5s), operator kept losing leader election
additionalArgs:
  - --leader-lease-duration=60
  - --leader-renew-deadline=40

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    memory: 512Mi
