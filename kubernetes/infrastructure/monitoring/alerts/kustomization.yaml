apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

commonAnnotations:
  argocd.argoproj.io/sync-wave: "6"

commonLabels:
  app.kubernetes.io/part-of: monitoring
  app.kubernetes.io/managed-by: kustomize

resources:
  - p0-critical.yaml
  - p1-infrastructure.yaml
  - p2-platform.yaml
  - p3-informational.yaml
  - argocd-applications.yaml
  - alertmanager-config.yaml
  - slack-webhook-sealed.yaml           # Alertmanager Slack webhook (monitoring namespace)
  # NOTE: argocd-slack-webhook-sealed.yaml moved to controllers/argocd/ (namespace must be argocd)
