apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 5: Jaeger Tracing (After Storage)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

resources:
  - ns.yaml
  # observability-ns.yaml removed - managed by observability application
  - manifests/jaeger-operator-v1.65.0.yaml  # Enterprise: Local versioned manifests
  - badger-pvc.yaml
  - jaeger.yaml