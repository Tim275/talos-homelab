apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: monitoring-

# 🌊 Sync Wave 5: Jaeger Tracing (After Storage)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

resources:
  - application.yaml           # ✅ Child Application for App-of-Apps pattern
  # 🛡️ POD SECURITY: Baseline compliant namespace
  - pod-security-exception.yaml  # Replaces ns.yaml with Pod Security labels

  # 🔧 JAEGER STACK
  - manifests/jaeger-operator-v1.65.0.yaml  # Enterprise: Local versioned manifests
  # - badger-pvc.yaml  # ❌ REMOVED: Using memory storage, PVC not needed
  - jaeger.yaml

namespace: jaeger