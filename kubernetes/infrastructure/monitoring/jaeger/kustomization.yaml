apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 5: Jaeger Tracing (After Storage)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

resources:
  # 🛡️ POD SECURITY: Privileged exception for Jaeger Agent hostPort
  - pod-security-exception.yaml  # Replaces ns.yaml with Pod Security labels

  # 🔧 JAEGER STACK
  - manifests/jaeger-operator-v1.65.0.yaml  # Enterprise: Local versioned manifests
  - badger-pvc.yaml
  - jaeger.yaml

namespace: jaeger