apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 📊 CONFIGMAP DASHBOARDS - Native Grafana Operator CRDs
# Links traditional ConfigMap dashboards to GrafanaDashboard CRDs

resources:
  - vector-dashboard-crd.yaml
  - sealed-secrets-dashboard-crd.yaml
