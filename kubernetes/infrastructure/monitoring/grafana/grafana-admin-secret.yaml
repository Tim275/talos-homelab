# 🔐 GRAFANA ADMIN CREDENTIALS - Fixed Authentication
#
# 🎯 CRITICAL: Fixes authentication issues between Grafana Operator and Grafana instance
#
# PROBLEM SOLVED:
# - Grafana Operator gets 401 Unauthorized errors
# - Dashboards show "NO MATCHING INSTANCES"
# - ServiceMonitors can't sync with Grafana
#
# FIXED CREDENTIALS:
# - Username: admin
# - Password: 2206
# - Consistent across all restarts and deployments
#
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-credentials
  namespace: monitoring
  labels:
    app.kubernetes.io/name: grafana-admin-secret
    app.kubernetes.io/component: authentication
    app.kubernetes.io/part-of: grafana-operator
    team: platform
type: Opaque
data:
  # admin (base64 encoded)
  username: YWRtaW4=
  # 2206 (base64 encoded)
  password: MjIwNg==