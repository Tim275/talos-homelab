
# ✅ WORKING REDIS DASHBOARD - For ArgoCD Redis monitoring
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: redis-monitoring-working
  namespace: monitoring
  labels:
    app.kubernetes.io/name: redis-dashboard
    app.kubernetes.io/part-of: grafana-operator
spec:
  instanceSelector:
    matchLabels:
      app: grafana  # ✅ Matches your Grafana instance
  folder: "Platform/Data"
  grafanaCom:
    id: 11835  # ✅ Redis Monitoring
    revision: 1
