apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: k8s-control-plane-scheduler
  namespace: grafana
  labels:
    app: grafana
    tier: tier-0
    component: control-plane
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  folder: "☸️ Kubernetes"
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
  url: "https://grafana.com/api/dashboards/12130/revisions/1/download"
