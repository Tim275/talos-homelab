apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: k8s-cluster-comprehensive
  namespace: grafana
  labels:
    app: grafana
    tier: tier-1
    component: cluster-monitoring
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  folder: "☸️ Kubernetes"
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
  url: "https://grafana.com/api/dashboards/315/revisions/3/download"
