apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🎯 VERIFIED WORKING DASHBOARDS ONLY
# ====================================

resources:
  # 📊 ARGOCD (5 dashboards)
  - argocd/argocd-app.yaml
  - argocd/argocd-operational.yaml
  - argocd/argocd-official.yaml
  - argocd/argocd-notifications.yaml

  # 💾 CEPH (Rook-Ceph verified by @galexrt)
  - ceph/rook-ceph-cluster.yaml
  - ceph/rook-ceph-pools.yaml
  - ceph/rook-ceph-osd.yaml

  # 🌐 CILIUM (v1.12 official)
  - cilium/cilium-agent.yaml
  - cilium/cilium-operator.yaml
  - cilium/cilium-hubble.yaml

  # ☸️ KUBERNETES - Complete Cluster Monitoring
  # === Global Views (dotdc) ===
  - kubernetes/k8s-views-global.yaml
  - kubernetes/k8s-views-namespaces.yaml
  - kubernetes/k8s-views-nodes.yaml
  - kubernetes/k8s-views-pods.yaml

  # === Control Plane Components (Tier-0 CRITICAL) ===
  - kubernetes/k8s-system-api-server.yaml
  - kubernetes/k8s-control-plane-scheduler.yaml
  - kubernetes/k8s-control-plane-controller-manager.yaml
  - kubernetes/k8s-control-plane-kubelet.yaml

  # === System Services ===
  - kubernetes/k8s-system-coredns.yaml
  - kubernetes/k8s-addons-prometheus.yaml

  # === Comprehensive Cluster Dashboard ===
  - kubernetes/k8s-cluster-comprehensive.yaml

  # 🔐 SEALED SECRETS (Bitnami official)
  - sealed-secrets/sealed-secrets-controller.yaml

  # 📨 KAFKA (Strimzi official - KRaft mode)
  - kafka/strimzi-kafka.yaml
  - kafka/strimzi-kraft.yaml
  - kafka/strimzi-kafka-exporter.yaml
  - kafka/strimzi-kafka-connect.yaml
  - kafka/strimzi-operators.yaml

  # 🐘 POSTGRESQL (CloudNativePG only)
  - postgresql/cloudnativepg-cluster.yaml

  # 🔍 ELASTICSEARCH (elasticsearch_exporter)
  - elasticsearch/elasticsearch-exporter-quickstart.yaml
  - elasticsearch/elasticsearch-cluster.yaml
  - elasticsearch/elasticsearch-stats.yaml
  - elasticsearch/elasticsearch-node-stats.yaml
  - elasticsearch/elasticsearch-exporter.yaml

  # 📝 LOKI (Promtail + Loki)
  - loki/loki-stack-monitoring.yaml
  - loki/loki-logs-dashboard.yaml
  - loki/loki-promtail.yaml
  - loki/loki-logging-v2.yaml
  - loki/loki-dashboard.yaml

  # 🔭 JAEGER (Distributed Tracing)
  - jaeger/jaeger-all-in-one.yaml
  - jaeger/jaeger-query.yaml
  - jaeger/jaeger-collector.yaml
  - jaeger/jaeger-complete.yaml
  - jaeger/jaeger-traces-metrics.yaml

  # 🛰️ OPENTELEMETRY (OTel Collector)
  - opentelemetry/otel-collector.yaml
  - opentelemetry/otel-apm.yaml
  - opentelemetry/otel-lightweight-apm.yaml
  - opentelemetry/otel-data-flow.yaml
  - opentelemetry/otel-jvm.yaml

  # 🦀 VECTOR (Log Collection)
  - vector/vector-monitoring.yaml
  - vector/vector-stats.yaml
  - vector/vector-full.yaml
  - vector/vector-cluster.yaml
  - vector/vector-kubernetes-logging.yaml

  # ⚙️ TALOS LINUX (Control Plane + Node Exporter + ETCD)
  - talos/talos-node-exporter-full.yaml
  - talos/talos-k8s-node-metrics.yaml
  - talos/talos-etcd.yaml
  - talos/talos-control-plane.yaml
  - talos/talos-cluster-health.yaml

  # 📱 N8N (Workflow Automation - Production Grade)
  # 🚀 Ultimate Dashboard (K8s Resources + Node.js + Network + GC)
  - n8n/n8n-production-ultimate.yaml
  # ServiceMonitors
  - n8n/n8n-prod-servicemonitor.yaml
  - n8n/n8n-dev-servicemonitor.yaml

namespace: grafana
