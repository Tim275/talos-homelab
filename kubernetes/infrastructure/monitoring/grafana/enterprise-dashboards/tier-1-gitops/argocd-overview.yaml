apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: argocd-overview
  namespace: grafana
  labels:
    app: grafana
    tier: tier-1
    category: gitops
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  allowCrossNamespaceImport: false

  # 🚀 ArgoCD Overview - Official ArgoCD Dashboard
  # Dashboard ID 14584 (130k+ downloads)
  # Maintained by: ArgoCD project
  grafanaCom:
    id: 14584
    revision: 1

  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"

  folder: "🔧 Tier-1: GitOps"
