apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: n8n-prod-nodejs-metrics
  namespace: grafana
  labels:
    app: n8n
    environment: production
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  folder: "📱 Applications"
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
  grafanaCom:
    id: 11159
    revision: 1
