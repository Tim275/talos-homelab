
# 🏢 UNPOLLER NETWORK SITES - Multi-Site Network Overview
# Site-to-site connectivity, branch office monitoring, WAN performance
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: unpoller-network-sites-monitoring
  namespace: monitoring
  labels:
    app.kubernetes.io/name: unpoller-network-dashboard
    app.kubernetes.io/part-of: grafana-operator
    app.kubernetes.io/component: infrastructure-monitoring
spec:
  instanceSelector:
    matchLabels:
      app: grafana  # ✅ Matches your Grafana instance
  folder: "Infrastructure/UniFi"
  grafanaCom:
    id: 11311  # ✅ UniFi Poller Network Sites
    revision: 5
