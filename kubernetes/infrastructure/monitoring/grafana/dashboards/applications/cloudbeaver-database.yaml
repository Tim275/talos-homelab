apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cloudbeaver-database
  namespace: grafana
  labels:
    app.kubernetes.io/part-of: monitoring-stack
    tier: applications
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      app: grafana
  folder: "CloudBeaver"
  resyncPeriod: 10m
  configMapRef:
    name: cloudbeaver-database-dashboard
    key: dashboard.json
