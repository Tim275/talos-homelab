apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: authelia-identity
  namespace: grafana
  labels:
    app.kubernetes.io/part-of: monitoring-stack
    tier: security
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      app: grafana
  folder: "Authelia"
  resyncPeriod: 10m
  instanceSelector:
    matchLabels:
      app: grafana
  configMapRef:
    name: authelia-identity-dashboard
    key: dashboard.json
