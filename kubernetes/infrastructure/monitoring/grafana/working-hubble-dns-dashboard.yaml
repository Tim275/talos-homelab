---
# 🌐 HUBBLE DNS DASHBOARD - Network DNS Monitoring
# DNS query patterns, latency, and network security insights
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: hubble-dns-namespace-monitoring
  namespace: monitoring
  labels:
    app.kubernetes.io/name: hubble-dns-dashboard
    app.kubernetes.io/part-of: grafana-operator
    app.kubernetes.io/component: network-monitoring
spec:
  instanceSelector:
    matchLabels:
      app: grafana  # ✅ Matches your Grafana instance
  folder: "Platform/Networking"
  grafanaCom:
    id: 16611  # ✅ Cilium Hubble DNS Metrics
    revision: 1
