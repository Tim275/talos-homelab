
# 📊 GRAFANA OPERATOR DASHBOARD - Monitoring Infrastructure Health
# Operator performance, dashboard sync status, resource management
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: grafana-operator-monitoring
  namespace: monitoring
  labels:
    app.kubernetes.io/name: grafana-operator-dashboard
    app.kubernetes.io/part-of: grafana-operator
    app.kubernetes.io/component: operator-monitoring
spec:
  instanceSelector:
    matchLabels:
      app: grafana  # ✅ Matches your Grafana instance
  folder: "Platform/Operators"
  grafanaCom:
    id: 13659  # ✅ Kubernetes Operator Monitoring
    revision: 2
