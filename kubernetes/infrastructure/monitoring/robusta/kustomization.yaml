apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 5: Monitoring Components (AI Alert Enrichment)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

metadata:
  name: robusta
  namespace: monitoring

helmCharts:
  - name: robusta
    repo: https://robusta-charts.storage.googleapis.com
    version: 0.28.1  # Latest stable version (Oct 2025)
    releaseName: robusta
    namespace: monitoring
    valuesFile: values.yaml

resources:
  # 🔐 Sealed Secrets
  - slack-webhook-sealed-secret.yaml  # Slack webhook URL for enriched alerts
