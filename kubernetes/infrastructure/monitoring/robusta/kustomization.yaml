apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 5: Monitoring Components (AI Alert Enrichment)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

metadata:
  name: robusta
  namespace: monitoring

helmCharts:
  - name: robusta
    repo: https://robusta-charts.storage.googleapis.com
    version: 0.15.2  # Latest stable version (Jan 2025)
    releaseName: robusta
    namespace: monitoring
    valuesFile: values.yaml

# resources:
#   # 🔐 Sealed Secrets (to be created)
#   # - holmesgpt-openai-api-key-sealed.yaml
#   # - robusta-slack-webhook-sealed.yaml  # Optional, if different from AlertManager
