apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 0: Prometheus Monitoring Stack (After CRDs in Wave -2)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"

resources:
  # 🎯 LEVEL 3: COMPONENT CONTROL - Comment/Uncomment for fine-grained control
  # CRDs now managed by prometheus-operator-crds Helm chart
  - ../alert-rules-grafana/      # ✅ ENABLED - Alerting rules (talos, kubernetes, production)

  # ❌ DISABLED ADVANCED CONFIGS - Uncomment when needed
  # - istio-monitoring.yaml     # ❌ Disabled - requires Istio integration
  # - kafka-monitoring.yaml     # ❌ Disabled - requires Kafka deployment
  # - kafka-metrics.yaml        # ❌ Disabled - requires Kafka metrics
# Namespace managed by parent monitoring app
# CRDs installed separately in ./crds to avoid annotation size issue
#
# ⚠️  IMPORTANT: monitoring namespace requires privileged PodSecurity for node-exporter
# See TALOS_SECURITY.md for details and Talos Linux specific requirements

helmCharts:
  # ✅ kube-prometheus-stack (main monitoring stack)
  # CRDs deployed separately via infrastructure-prometheus-crds
  - name: kube-prometheus-stack
    repo: https://prometheus-community.github.io/helm-charts
    version: "77.11.0" # renovate: helm=prometheus-community/kube-prometheus-stack
    releaseName: prometheus-operator
    namespace: monitoring
    valuesFile: values.yaml
    includeCRDs: false  # CRDs handled by separate infrastructure-prometheus-crds application
