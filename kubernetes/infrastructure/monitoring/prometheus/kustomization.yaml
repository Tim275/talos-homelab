apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 5: Prometheus Monitoring Stack (After Storage)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

resources:
  # Only basic configs - no CRDs, no Prometheus/AlertManager CRs
  # - istio-monitoring.yaml     # Disabled - requires Prometheus CRDs
  # - kafka-monitoring.yaml     # Disabled - requires Prometheus CRDs
  # - kafka-metrics.yaml        # Disabled - requires Prometheus CRDs
# Namespace managed by parent monitoring app
# CRDs installed separately in ./crds to avoid annotation size issue
#
# ⚠️  IMPORTANT: monitoring namespace requires privileged PodSecurity for node-exporter
# See TALOS_SECURITY.md for details and Talos Linux specific requirements

helmCharts:
  # ✅ ONLY kube-prometheus-stack (industry standard, all-in-one)
  - name: kube-prometheus-stack
    repo: https://prometheus-community.github.io/helm-charts
    version: "65.1.1" # renovate: helm=prometheus-community/kube-prometheus-stack
    releaseName: prometheus-operator
    namespace: monitoring
    valuesFile: values.yaml
    includeCRDs: false  # CRDs disabled - too large annotations cause K8s API errors