apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # - ns.yaml                        # ❌ DISABLED: namespace created by namespace-monitoring application
  # - alertmanager-http-route.yaml   # ❌ DISABLED: Start simple like Vegarn, add routes later
  # - prometheus-http-route.yaml     # ❌ DISABLED: Start simple like Vegarn, add routes later

helmCharts:
  - name: kube-prometheus-stack
    repo: https://prometheus-community.github.io/helm-charts
    includeCRDs: true
    namespace: monitoring
    version: 75.15.1
    releaseName: kube-prometheus-stack
    valuesFile: ./values.yaml
