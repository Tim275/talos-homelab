# 🚫 ArgoCD Ignore - This directory should NOT be deployed
#
# This is an ARCHIVED version of the old Helm-managed Grafana setup
# kept for reference and emergency rollback only.
#
# The current ApplicationSet uses explicit list generator, so this
# directory won't be discovered anyway, but this file provides
# extra insurance against accidental deployment.

*