apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cluster-dashboards.yaml
  - specialized-dashboards.yaml  # Talos, MongoDB, ArgoCD, Ceph, Health, Cilium
  - n8n/

commonLabels:
  app.kubernetes.io/part-of: grafana
  app.kubernetes.io/component: dashboards