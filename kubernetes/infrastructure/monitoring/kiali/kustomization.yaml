apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Sync Wave 5: Kiali Service Mesh Observability
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

namespace: istio-system

resources:
  - serviceaccount.yaml
  - rbac.yaml
  - clusterrolebinding.yaml
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: kiali
    files:
      - config.yaml
