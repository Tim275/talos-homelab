apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - pagerduty-integration-key-sealed.yaml
  - kubernetes-critical-alerts.yaml
  - argocd-slack-webhook-sealed.yaml
  - argocd-slo-alerts.yaml
  - storage-slack-webhook-sealed.yaml
  - storage-alerts.yaml
  - database-slack-webhook-sealed.yaml
  - database-alerts.yaml
  - security-slack-webhook-sealed.yaml
  - security-alerts.yaml

commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"
