apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - pagerduty-integration-key-sealed.yaml
  - kubernetes-critical-alerts.yaml

commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"
