---
apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    pod-security.kubernetes.io/enforce: privileged
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
# 📋 NOTE: Namespace grafana is managed by the operators app
# (kubernetes/infrastructure/controllers/operators/namespaces.yaml)
# to avoid SharedResourceWarning conflicts in ArgoCD.