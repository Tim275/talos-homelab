apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

# 🌊 Sync Wave 7: Monitoring Dashboards (After Prometheus is ready)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "7"

resources:
  - application.yaml
  - rook-ceph-dashboards.yaml
  - cilium-hubble-dashboards.yaml
  - cilium-vegarn-style.yaml      # 🚀 Cilium with configMapRef (Vegarn style)
  - istio-dashboards.yaml
  # - kafka-strimzi-cluster.yaml      # ❌ MOVED: Now managed by infrastructure-grafana ApplicationSet
  # - elasticsearch-cluster.yaml      # ❌ MOVED: Now managed by infrastructure-grafana ApplicationSet
  - n8n-dashboards.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: monitoring-stack
      app.kubernetes.io/component: monitoring-dashboards
