apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 5: Monitoring Components
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

metadata:
  name: keep
  namespace: monitoring

helmCharts:
  - name: keep
    repo: https://keephq.github.io/helm-charts
    version: 0.31.19  # Latest stable version
    releaseName: keep
    namespace: monitoring
    valuesFile: values.yaml

resources:
  - httproute.yaml
