apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Sync Wave 5: Monitoring Components
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

metadata:
  name: keep
  namespace: monitoring

helmCharts:
  - name: keep
    repo: https://keephq.github.io/helm-charts
    version: 0.1.94  # Latest version (App: 0.45.4)
    releaseName: keep
    namespace: monitoring
    valuesFile: values.yaml

resources:
  - httproute.yaml
