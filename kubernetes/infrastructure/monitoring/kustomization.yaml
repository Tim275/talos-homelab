apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🎯 TRUE KUSTOMIZE CONTROL - COMMENT/UNCOMMENT = ENABLE/DISABLE!
# ===============================================================
# This is what you wanted for 35 hours! Pure kustomize control!


# 🌊 Sync Wave 5: Monitoring (After Storage)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"

  # 🎯 MONITORING COMPONENTS - ENTERPRISE GOTEMPLATE CONTROL!
  # ===========================================================

  # 🏗️ INFRASTRUCTURE FOUNDATION (Wave 0)
resources:
  - alerts/application.yaml
  # - grafana-operator/application.yaml  # MOVED to controllers/operators
  - grafana/application.yaml
  - hubble/application.yaml
  - jaeger/application.yaml
  - kube-prometheus-stack/application.yaml
  - loki/application.yaml
  - metrics-server/application.yaml
  # - tempo/application.yaml  # Tempo manages its own Application
  # - servicemonitors/application.yaml       # ❌ REMOVED - ServiceMonitors consolidated into kube-prometheus-stack
  # - grafana/enterprise-dashboards/application.yaml  # ❌ DISABLED - Dashboards now in grafana/kustomization.yaml

  # 🤖 AI OPERATIONS - Self-hosted AI for alert enrichment
  # - ../ai-inference/ollama/application.yaml       # ❌ DISABLED - Self-hosted LLM (high CPU/memory)
  # - keep/application.yaml                         # ❌ REMOVED - MySQL incompatible with SealedSecrets
  # - robusta/application.yaml                      # ❌ DISABLED - AI alert enrichment (high resources)
