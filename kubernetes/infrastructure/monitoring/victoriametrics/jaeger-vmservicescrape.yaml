# 🎯 Jaeger VMServiceScrape - Distributed tracing metrics
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: jaeger-query
  namespace: monitoring
  labels:
    app: jaeger-query
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: jaeger-query
  namespaceSelector:
    matchNames:
      - jaeger
  endpoints:
    - port: admin-http
      path: /metrics
      interval: 30s
---
# 🎯 Jaeger Collector VMServiceScrape
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: jaeger-collector
  namespace: monitoring
  labels:
    app: jaeger-collector
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: jaeger-collector
  namespaceSelector:
    matchNames:
      - jaeger
  endpoints:
    - port: admin-http
      path: /metrics
      interval: 30s
---
# 🎯 Jaeger Agent VMServiceScrape
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: jaeger-agent
  namespace: monitoring
  labels:
    app: jaeger-agent
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: jaeger-agent
  namespaceSelector:
    matchNames:
      - jaeger
  endpoints:
    - port: admin-http
      path: /metrics
      interval: 30s