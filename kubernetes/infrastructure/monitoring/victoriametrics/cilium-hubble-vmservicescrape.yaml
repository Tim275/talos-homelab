# 🎯 Cilium & Hubble VMServiceScrape - Network observability metrics
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: cilium-agent
  namespace: kube-system
  labels:
    app: cilium-agent
spec:
  selector:
    matchLabels:
      k8s-app: cilium
  endpoints:
    - port: prometheus
      path: /metrics
      interval: 30s
---
# 🎯 Hubble Metrics VMServiceScrape
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: hubble-metrics
  namespace: kube-system
  labels:
    app: hubble-metrics
spec:
  selector:
    matchLabels:
      k8s-app: hubble
  endpoints:
    - port: hubble-metrics
      path: /metrics
      interval: 30s
---
# 🎯 Hubble Relay VMServiceScrape
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: hubble-relay
  namespace: kube-system
  labels:
    app: hubble-relay
spec:
  selector:
    matchLabels:
      k8s-app: hubble-relay
  endpoints:
    - port: prometheus
      path: /metrics
      interval: 30s