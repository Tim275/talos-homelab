# 🎯 Istio Control Plane VMServiceScrape - Service mesh metrics
# Note: istiod metrics path needs verification - disabled for now
---
# 🎯 Sail Operator VMServiceScrape - Istio Operator metrics
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: sail-operator
  namespace: monitoring
  labels:
    app: sail-operator
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: sail-operator
  namespaceSelector:
    matchNames:
      - istio-system
  endpoints:
    - port: https
      path: /metrics
      interval: 30s