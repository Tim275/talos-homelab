apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 📊 SERVICEMONITORS for Enterprise Dashboards
# Deployed in monitoring namespace for Prometheus discovery

resources:
  # - jaeger.yaml              # ❌ DISABLED - Jaeger Query has no metrics endpoint
  # - vector-aggregator.yaml   # ❌ DISABLED - No metrics exposed
  # - vector-agent.yaml        # ❌ DISABLED - No metrics exposed
  # - loki.yaml                # ❌ DISABLED - Loki has built-in ServiceMonitors from Helm
  - cnpg-clusters.yaml

namespace: monitoring
