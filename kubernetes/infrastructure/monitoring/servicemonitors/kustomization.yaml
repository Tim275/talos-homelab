apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 📊 SERVICEMONITORS for Enterprise Dashboards
# Deployed in monitoring namespace for Prometheus discovery

resources:
  # ✅ INFRASTRUCTURE MONITORING
  - servicemonitor-grafana.yaml             # Grafana monitoring UI metrics
  - servicemonitor-cert-manager.yaml        # SSL certificate health

  # ✅ APPLICATION MONITORING
  - servicemonitor-elasticsearch-exporter.yaml  # Elasticsearch cluster health
  - servicemonitor-kafka-exporter.yaml          # Kafka topic/consumer metrics
  - cnpg-clusters.yaml                          # PostgreSQL cluster metrics

namespace: monitoring
