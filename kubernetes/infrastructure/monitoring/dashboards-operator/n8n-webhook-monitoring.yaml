apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: n8n-webhook-monitoring
  namespace: grafana
  labels:
    app: grafana
    pattern: dotdc-cncf
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  allowCrossNamespaceImport: true
  resyncPeriod: "10m"
  folder: "N8N Automation"
  json: |
    {
      "title": "🌐 N8N Webhook Monitoring",
      "uid": "n8n-webhook-monitoring",
      "tags": ["n8n", "webhooks", "api", "checkmk"],
      "panels": [
        {
          "title": "🌐 Webhook Response Status",
          "type": "stat",
          "fieldConfig": {
            "defaults": {
              "color": {"mode": "thresholds"},
              "thresholds": {
                "steps": [
                  {"color": "red", "value": null},
                  {"color": "yellow", "value": 1},
                  {"color": "green", "value": 5}
                ]
              },
              "mappings": [
                {"options": {"0": {"text": "❌ NO WEBHOOKS", "color": "red"}}, "type": "value"},
                {"options": {"from": 1, "to": 4, "result": {"text": "⚠️ LOW TRAFFIC", "color": "yellow"}}, "type": "range"},
                {"options": {"from": 5, "to": 1000, "result": {"text": "✅ ACTIVE", "color": "green"}}, "type": "range"}
              ]
            }
          },
          "targets": [
            {
              "expr": "sum(rate(container_network_receive_packets_total{pod=~\".*n8n.*\"}[5m]))",
              "legendFormat": "Webhook Requests"
            }
          ]
        }
      ],
      "time": {"from": "now-1h", "to": "now"}
    }