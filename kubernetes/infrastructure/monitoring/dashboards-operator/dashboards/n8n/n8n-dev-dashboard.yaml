apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: n8n-dev-dashboard
  labels:
    app: grafana
    application: n8n
    environment: development
    component: workflow-automation
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  grafanaCom:
    id: 19467
    revision: 1
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
