apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: istio-traffic-management-dashboard
  labels:
    app: grafana
    application: istio
    component: traffic-management
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  grafanaCom:
    id: 7636
    revision: 173
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
