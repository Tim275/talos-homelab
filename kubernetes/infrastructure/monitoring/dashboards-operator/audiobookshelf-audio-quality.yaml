apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: audiobookshelf-audio-quality
  namespace: grafana
  labels:
    app: grafana
    pattern: dotdc-cncf
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  allowCrossNamespaceImport: true
  resyncPeriod: "10m"
  folder: "Audiobookshelf Media"
  json: |
    {
      "title": "🎵 Audiobookshelf Audio Quality",
      "uid": "audiobookshelf-audio-quality",
      "tags": ["audiobookshelf", "media", "audio", "quality"],
      "panels": [
        {
          "title": "🎵 Streaming Quality",
          "type": "timeseries",
          "targets": [
            {
              "expr": "sum(rate(container_network_transmit_bytes_total{pod=~\".*audiobookshelf.*\"}[5m])) / 1024",
              "legendFormat": "🎧 Audio Bitrate (KB/s)"
            }
          ]
        }
      ],
      "time": {"from": "now-1h", "to": "now"}
    }