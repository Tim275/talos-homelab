apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # 🎯 ENTERPRISE APPLICATION-BASED MONITORING
  # Each folder = One application with 3-5 community dashboards

  # Rook Ceph - Storage Platform
  - rook-ceph-cluster.yaml
  - rook-ceph-osd.yaml
  - rook-ceph-pools.yaml

  # Cilium - Network & Security
  - cilium-hubble-v112.yaml
  - cilium-policy-verdicts.yaml

  # ArgoCD - GitOps Platform
  - argocd-overview.yaml
  - argocd-operational.yaml
  - argocd-application.yaml

  # CloudNative PG - Database Platform
  - cloudnativepg-overview.yaml
  - postgresql-database.yaml

  # Kubernetes - Cluster Monitoring
  - kubernetes-cluster.yaml

  # Istio - Service Mesh
  - istio-mesh.yaml

  # Elasticsearch - Logging Platform
  - elasticsearch-overview.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  monitoring.tier: enterprise