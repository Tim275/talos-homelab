apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🎯 ENTERPRISE INFRASTRUCTURE AS CODE DASHBOARDS
# ================================================
# Based on DOTDC/CNCF/GitLab Production Patterns
# Clean Enterprise Monitoring - NO Chaos!

commonLabels:
  app.kubernetes.io/name: enterprise-dashboards
  app.kubernetes.io/component: monitoring
  app.kubernetes.io/part-of: observability-platform
  enterprise: "true"
  pattern: dotdc-cncf

# 🌊 Sync Wave 6: Dashboards (After Monitoring Infrastructure)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "6"

resources:
  # 📈 ENTERPRISE INFRASTRUCTURE MONITORING
  # ======================================
  - global-infrastructure-overview.yaml

  # Future: Add other enterprise dashboards here as needed
  # - talos-kubernetes-control-plane-overview.yaml
  # - rook-ceph-storage-cluster-overview.yaml
  # etc.