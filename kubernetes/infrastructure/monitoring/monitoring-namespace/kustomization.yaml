apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: monitoring-

# 🏗️ Monitoring Namespace - Must be created first!
# Sync Wave 0 to ensure namespace exists before all monitoring services

resources:
  - namespace.yaml