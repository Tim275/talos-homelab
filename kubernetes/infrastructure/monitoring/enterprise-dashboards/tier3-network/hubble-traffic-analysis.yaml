apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: hubble-traffic-analysis
  labels:
    app: grafana
    tier: network
    component: hubble-monitoring
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  folder: "🌐 TIER-3: Network & Connectivity"
  grafanaCom:
    id: 16612
    revision: 1
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "prometheus"
