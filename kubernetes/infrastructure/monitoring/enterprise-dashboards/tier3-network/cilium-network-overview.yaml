apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: cilium-network-overview
  labels:
    app: grafana
    tier: network
    component: cilium-monitoring
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  folder: "🌐 TIER-3: Network & Connectivity"
  grafanaCom:
    id: 16611
    revision: 1
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "prometheus"
