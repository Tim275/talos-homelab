apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: sealed-secrets-management
  labels:
    app: grafana
    tier: security
    component: secrets-monitoring
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  folder: "🔐 TIER-2: Security & Compliance"
  grafanaCom:
    id: 15039
    revision: 2
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "prometheus"
