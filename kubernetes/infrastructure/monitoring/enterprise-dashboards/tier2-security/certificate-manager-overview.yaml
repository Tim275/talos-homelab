apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: certificate-manager-overview
  labels:
    app: grafana
    tier: security
    component: certificate-monitoring
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  folder: "🔐 TIER-2: Security & Compliance"
  grafanaCom:
    id: 11001
    revision: 1
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "prometheus"
