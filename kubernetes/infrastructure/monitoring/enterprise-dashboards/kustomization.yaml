apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: grafana

resources:
  # 🏗️ TIER 1: INFRASTRUCTURE MONITORING
  - tier1-infrastructure/kubernetes-cluster-overview.yaml
  - tier1-infrastructure/node-exporter-comprehensive.yaml
  - tier1-infrastructure/etcd-cluster-health.yaml
  - tier1-infrastructure/kube-apiserver-performance.yaml

  # 🔐 TIER 2: SECURITY & COMPLIANCE
  - tier2-security/certificate-manager-overview.yaml
  - tier2-security/sealed-secrets-management.yaml

  # 🌐 TIER 3: NETWORK & CONNECTIVITY
  - tier3-network/cilium-network-overview.yaml
  - tier3-network/hubble-traffic-analysis.yaml

  # 💾 TIER 4: STORAGE & DATA
  - tier4-storage/rook-ceph-cluster-health.yaml

  # 🎯 TIER 5: PLATFORM SERVICES
  - tier5-platform/argocd-gitops-overview.yaml

commonLabels:
  app.kubernetes.io/name: enterprise-dashboards
  app.kubernetes.io/component: monitoring
  app.kubernetes.io/part-of: observability-platform
