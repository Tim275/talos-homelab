apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: argocd-gitops-overview
  labels:
    app: grafana
    tier: platform
    component: gitops-monitoring
spec:
  instanceSelector:
    matchLabels:
      app: grafana
  folder: "🎯 TIER-5: Platform Services"
  grafanaCom:
    id: 14584
    revision: 1
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "prometheus"
