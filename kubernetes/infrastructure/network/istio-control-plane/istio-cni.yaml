apiVersion: sailoperator.io/v1
kind: IstioCNI
metadata:
  name: default
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  version: v1.30.0
  namespace: istio-cni
  values:
    cni:
      ambient:
        enabled: true
      excludeNamespaces:
        - istio-system
        - istio-cni
        - kube-system
