# Istio CNI for Ambient Mode
# Managed by Sail Operator - installs CNI plugin on each node
apiVersion: sailoperator.io/v1
kind: IstioCNI
metadata:
  name: default
spec:
  version: v1.27.1
  profile: ambient
  namespace: istio-cni
