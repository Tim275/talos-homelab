apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌐 GRANULAR CONTROL: Istio Base Only (CRDs)
namespace: argocd

resources:
  - ../istio-base/kustomization.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: network-istio-base
  enterprise.granular: istio-base-only