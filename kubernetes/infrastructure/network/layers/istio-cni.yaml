apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌐 GRANULAR CONTROL: Istio CNI Only
namespace: argocd

resources:
  - ../istio-cni/kustomization.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: network-istio-cni
  enterprise.granular: istio-cni-only