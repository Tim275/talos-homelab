apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌐 GRANULAR CONTROL: Istio Control Plane Only (Istiod)
namespace: argocd

resources:
  - ../istio-control-plane/kustomization.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: network-istio-control-plane
  enterprise.granular: istio-control-plane-only