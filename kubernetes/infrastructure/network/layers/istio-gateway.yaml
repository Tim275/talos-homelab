apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌐 GRANULAR CONTROL: Istio Gateway Only
namespace: argocd

resources:
  - ../istio-gateway/kustomization.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: network-istio-gateway
  enterprise.granular: istio-gateway-only