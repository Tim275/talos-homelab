# istio-cni — PARKED

Siehe `kubernetes/infrastructure/network/istio-base/PARKED.md` für die volle Erklärung.

**Status:** auskommentiert/nicht-referenziert in `network/kustomization.yaml`. Folder bleibt für Restore-Pfad.
