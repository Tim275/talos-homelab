apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 3: Istio Gateway (after Control Plane)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "3"

namespace: istio-system

resources:
  - simple-gateway.yaml