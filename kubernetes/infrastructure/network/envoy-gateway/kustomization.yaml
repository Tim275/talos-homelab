apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 1: Network Components (Gateway API Implementation)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "1"

metadata:
  name: envoy-gateway
  namespace: envoy-gateway-system

resources:
- namespace.yaml
- envoy-gateway-deployment.yaml