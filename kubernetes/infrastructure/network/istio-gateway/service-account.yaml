apiVersion: v1
kind: ServiceAccount
metadata:
  name: istio-gateway
  namespace: istio-ingress
  labels:
    app: istio-gateway
    istio: gateway
    app.kubernetes.io/name: istio-gateway
    app.kubernetes.io/instance: istio-gateway
