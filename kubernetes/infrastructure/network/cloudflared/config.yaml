tunnel: b5f4258e-8cd9-4454-b46e-6f4f34219bb4
credentials-file: /etc/cloudflared/credentials/credentials.json
metrics: 0.0.0.0:2000
no-autoupdate: true

warp-routing:
  enabled: true

ingress:
  # All services via Envoy Gateway (Best Practice - Vegarn Style)
  - hostname: "*.timourhomelab.org"
    service: https://envoy-gateway-envoy-gateway-ee418b6e.envoy-gateway-system.svc.cluster.local:443
    originRequest:
      httpHostHeader: "{http.request.host}"
      noTLSVerify: true

  - hostname: timourhomelab.org
    service: https://envoy-gateway-envoy-gateway-ee418b6e.envoy-gateway-system.svc.cluster.local:443
    originRequest:
      httpHostHeader: "{http.request.host}"
      noTLSVerify: true

  - service: http_status:404
