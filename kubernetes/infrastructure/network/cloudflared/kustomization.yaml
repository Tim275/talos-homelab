apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 3: Tunnel Services (After certificates are ready)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "3"

configMapGenerator:
  - name: cloudflared-config
    namespace: cloudflared
    files:
      - config.yaml

resources:
  - ns.yaml
  - sealed-credentials.yaml
  - daemon-set.yaml