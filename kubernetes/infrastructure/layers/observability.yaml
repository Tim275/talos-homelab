apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🔍 GRANULAR CONTROL: Observability Layer Only
namespace: argocd

resources:
  - ../infrastructure-observability.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: infrastructure-observability
  enterprise.granular: observability-only