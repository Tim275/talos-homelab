apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 💾 GRANULAR CONTROL: Storage Layer Only
namespace: argocd

resources:
  - ../infrastructure-storage.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: infrastructure-storage
  enterprise.granular: storage-only