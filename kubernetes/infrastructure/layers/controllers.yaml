apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🎮 GRANULAR CONTROL: Controllers Layer Only
namespace: argocd

resources:
  - ../infrastructure-controllers.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: infrastructure-controllers
  enterprise.granular: controllers-only