apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌐 GRANULAR CONTROL: Network Layer Only
namespace: argocd

resources:
  - ../infrastructure-network.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: infrastructure-network
  enterprise.granular: network-only