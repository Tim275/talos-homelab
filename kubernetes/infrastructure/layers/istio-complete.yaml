apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌐 GRANULAR CONTROL: Complete Istio Service Mesh
namespace: argocd

resources:
  - ../istio/kustomization.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: istio-complete
  enterprise.granular: istio-service-mesh