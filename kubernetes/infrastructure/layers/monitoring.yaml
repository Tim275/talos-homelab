apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 📊 GRANULAR CONTROL: Monitoring Layer Only
namespace: argocd

resources:
  - ../infrastructure-monitoring.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: infrastructure-monitoring
  enterprise.granular: monitoring-only