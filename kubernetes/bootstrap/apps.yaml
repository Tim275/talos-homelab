# 🚀 BOOTSTRAP - App-of-Apps Pattern (2025 Enterprise Best Practice)
# =================================================================
# Bootstrap application for Apps layer

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apps
  namespace: argocd
  labels:
    enterprise.tier: bootstrap
    bootstrap.layer: apps
    team: timour
  annotations:
    argocd.argoproj.io/sync-wave: "25"
spec:
  project: apps
  source:
    repoURL: https://github.com/Tim275/talos-homelab
    targetRevision: HEAD
    path: kubernetes/apps
    kustomize: {}
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
