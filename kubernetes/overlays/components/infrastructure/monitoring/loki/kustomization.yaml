apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: loki-component
  annotations:
    infrastructure.config/component: "loki"
    infrastructure.config/description: "Loki log aggregation system"

resources:
  # Point to infra implementation
  - ../../../../../../infra/monitoring/loki

labels:
  - pairs:
      component: loki
      tier: monitoring
      deployment-method: granular-kustomize