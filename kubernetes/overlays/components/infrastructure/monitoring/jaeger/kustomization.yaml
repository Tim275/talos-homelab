apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: jaeger-component
  annotations:
    infrastructure.config/component: "jaeger"
    infrastructure.config/description: "Jaeger distributed tracing"

resources:
  # Point to infra implementation - DISABLED for now
  # - ../../../../../../infra/monitoring/jaeger

# patches:
#   - target:
#       kind: Application
#       name: jaeger
#     patch: |-
#       - op: replace
#         path: /spec/syncPolicy/automated
#         value: null

labels:
  - pairs:
      component: jaeger
      tier: monitoring
      deployment-method: granular-kustomize
      status: disabled