apiVersion: v1
kind: Service
metadata:
  name: media-server
  labels:
    app: media-server
spec:
  selector:
    app: media-server
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP