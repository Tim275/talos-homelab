apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: chaos-mesh

resources:
  - namespace.yaml

helmCharts:
  - name: chaos-mesh
    repo: https://charts.chaos-mesh.org
    version: 2.7.0  # renovate: github-releases=chaos-mesh/chaos-mesh
    releaseName: chaos-mesh
    namespace: chaos-mesh
    includeCRDs: true
    valuesFile: values.yaml
