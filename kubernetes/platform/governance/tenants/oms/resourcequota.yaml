apiVersion: v1
kind: ResourceQuota
metadata:
  name: oms-quota
  namespace: oms
spec:
  hard:
    requests.cpu: "4"
    requests.memory: "8Gi"
    limits.cpu: "8"
    limits.memory: "16Gi"
    pods: "50"
    services: "20"
    configmaps: "10"
    secrets: "10"
    persistentvolumeclaims: "10"
    requests.storage: "100Gi"
