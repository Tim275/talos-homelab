apiVersion: v1
kind: ResourceQuota
metadata:
  name: oms-quota
  namespace: oms
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    persistentvolumeclaims: "10"
    services.loadbalancers: "0"
