apiVersion: v1
kind: ResourceQuota
metadata:
  name: oms-quota
  namespace: oms
spec:
  hard:
    requests.cpu: "12"
    requests.memory: 24Gi
    limits.cpu: "24"
    limits.memory: 48Gi
    persistentvolumeclaims: "20"
    services.loadbalancers: "0"
