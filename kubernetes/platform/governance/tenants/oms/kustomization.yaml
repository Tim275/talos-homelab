apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- resourcequota.yaml
- limitrange.yaml
- rbac.yaml

labels:
- pairs:
    platform.homelab/tenant: oms
    platform.homelab/managed-by: platform-admin
    tenant.type: production
