apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - redis-drova-app.yaml

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      platform.homelab/tenant: drova
      platform.domain: drova-infra
