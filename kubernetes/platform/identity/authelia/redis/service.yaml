apiVersion: v1
kind: Service
metadata:
  name: redis-authelia
  namespace: redis-authelia
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: session-store
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    app.kubernetes.io/name: redis
