apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


namespace: authelia

resources:
  # Redis Operator CR (NEW - replaces deployment.yaml)
  - redis-operator.yaml
  # Service is created by operator automatically
  # - service.yaml  # ← Removed, operator creates service
  - sealed-secrets.yaml

commonLabels:
  app.kubernetes.io/name: redis-authelia
  app.kubernetes.io/component: session-store
  app.kubernetes.io/part-of: platform-identity
  app.kubernetes.io/managed-by: redis-operator
