---
# Infisical Non-Secret Configuration
# Static values that don't need encryption
apiVersion: v1
kind: ConfigMap
metadata:
  name: infisical-config
  namespace: infisical
data:
  NODE_ENV: "production"
