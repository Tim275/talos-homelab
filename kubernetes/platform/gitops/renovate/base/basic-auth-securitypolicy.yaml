apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: renovate-basic-auth
  namespace: renovate-operator
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: renovate-operator-renovate-operator
  basicAuth:
    users:
      name: renovate-basic-auth
