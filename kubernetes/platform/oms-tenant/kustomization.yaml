apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# OMS Tenant - Platform-Provided Resources
# This includes all infrastructure the platform team provides for OMS

labels:
  - pairs:
      app.kubernetes.io/managed-by: platform-team
      platform.homelab/tenant: oms
      enterprise.tier: platform

resources:
  - connection-config/
