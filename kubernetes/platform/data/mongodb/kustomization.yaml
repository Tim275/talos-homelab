apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# MongoDB Operator + Clusters
# Complete MongoDB stack: operator and database clusters

resources:
  # MongoDB Operator (sync-wave 1-2)
  - namespace.yaml
  - operator.yaml
  # MongoDB Clusters (sync-wave 3)  
  - mongodb-cluster.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: mongodb-stack