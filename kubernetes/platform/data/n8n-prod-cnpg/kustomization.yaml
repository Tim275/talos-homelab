apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


# 🌊 Sync Wave 12: Database Clusters (After Operators)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "12"

resources:
  - namespace.yaml
  - credentials-secret.yaml
  - backup-s3-credentials.yaml
  - cluster.yaml  # Daily base backups (no WAL archiving)
  # - cluster-with-wal.yaml  # ⚠️ OPTIONAL: Enable for Point-in-Time Recovery (PITR)
  - scheduled-backup.yaml
  # CloudBeaver is now a separate platform component for database administration

labels:
  - pairs:
      app.kubernetes.io/part-of: n8n-data
