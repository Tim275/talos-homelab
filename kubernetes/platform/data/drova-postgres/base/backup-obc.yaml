apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: cnpg-drova-backup
  namespace: drova
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  bucketName: cnpg-drova-backup
  storageClassName: rook-ceph-bucket
