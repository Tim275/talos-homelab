apiVersion: v1
kind: Service
metadata:
  name: redis-n8n-master
  namespace: n8n-prod
  labels:
    app: redis-n8n
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      name: redis
  selector:
    app: redis-n8n
