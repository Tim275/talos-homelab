apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # - postgres-cluster.yaml  # Disabled - no CloudNative-PG operator
  # - pgadmin.yaml          # Disabled - no database

labels:
  - pairs:
      app.kubernetes.io/part-of: n8n-data