apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - postgres-cluster.yaml
  # pgAdmin is now a separate platform component

labels:
  - pairs:
      app.kubernetes.io/part-of: n8n-data