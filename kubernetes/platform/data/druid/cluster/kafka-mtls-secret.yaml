# Druid-Kafka mTLS RBAC
# The druid-kafka-mtls secret is created manually by copying from kafka namespace
# Run: kubectl get secret druid-analytics -n kafka -o json | jq '.metadata.namespace = "druid" | .metadata.name = "druid-kafka-mtls"' | kubectl apply -f -
---
# ServiceAccount for copying secrets (kept for manual use)
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kafka-secret-copier
  namespace: druid
---
# Role to read secrets from kafka namespace
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kafka-secret-reader
  namespace: kafka
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["druid-analytics"]
    verbs: ["get"]
---
# RoleBinding for kafka namespace
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: druid-kafka-secret-reader
  namespace: kafka
subjects:
  - kind: ServiceAccount
    name: kafka-secret-copier
    namespace: druid
roleRef:
  kind: Role
  name: kafka-secret-reader
  apiGroup: rbac.authorization.k8s.io
---
# Role to create secrets in druid namespace
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: druid-secret-writer
  namespace: druid
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "create", "update", "patch"]
---
# RoleBinding for druid namespace
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: druid-secret-writer
  namespace: druid
subjects:
  - kind: ServiceAccount
    name: kafka-secret-copier
    namespace: druid
roleRef:
  kind: Role
  name: druid-secret-writer
  apiGroup: rbac.authorization.k8s.io
