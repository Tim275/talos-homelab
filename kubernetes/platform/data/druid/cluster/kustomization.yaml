apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: druid
resources:
  - namespace.yaml
  - postgres-credentials.yaml
  - postgres-cluster.yaml
  # Enterprise Security
  - tls-certificates.yaml
  - tls-truststore.yaml
  - mtls-config.yaml
  - security-config.yaml
  - pod-security.yaml
  - audit-logging.yaml
  # OAuth2/OIDC
  - oauth2-proxy-secret.yaml
  - oauth2-proxy.yaml
  # Networking
  - httproute.yaml
  # Kafka Integration
  - analytics-topics.yaml
  - kafka-mtls-secret.yaml
  - kafka-supervisors.yaml
