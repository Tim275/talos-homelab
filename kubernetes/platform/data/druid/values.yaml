# Druid Helm Chart - Minimal Working Config
image:
  repository: apache/druid
  tag: "30.0.1"

# ZooKeeper
zookeeper:
  enabled: true
  replicaCount: 1
  image:
    registry: docker.io
    repository: bitnamilegacy/zookeeper
    tag: "3.8.4-debian-12-r0"
  persistence:
    enabled: true
    storageClass: rook-ceph-block-enterprise
    size: 2Gi

# Disable built-in PostgreSQL (use CNPG)
postgresql:
  enabled: false

# External metadata
metadataStore:
  type: postgresql
  host: druid-postgres-rw.druid.svc
  port: 5432
  user: druid
  password: druid-metadata-2026

# Deep storage
deepStorage:
  type: local

# Extensions
configVars:
  druid_extensions_loadList: '["postgresql-metadata-storage", "druid-kafka-indexing-service", "druid-datasketches", "druid-basic-security"]'
  druid_storage_type: local
  druid_storage_storageDirectory: /opt/druid/var/druid/segments

  # Authentication - User: admin, Password: anwar321
  druid_auth_authenticatorChain: '["BasicMetadataAuthenticator"]'
  druid_auth_authenticator_BasicMetadataAuthenticator_type: basic
  druid_auth_authenticator_BasicMetadataAuthenticator_initialAdminPassword: anwar321
  druid_auth_authenticator_BasicMetadataAuthenticator_initialInternalClientPassword: druid-internal-2026
  druid_auth_authenticator_BasicMetadataAuthenticator_credentialsValidator_type: metadata
  druid_auth_authenticator_BasicMetadataAuthenticator_skipOnFailure: "true"
  druid_auth_authenticator_BasicMetadataAuthenticator_authorizerName: BasicMetadataAuthorizer
  druid_auth_authenticator_BasicMetadataAuthenticator_enableCacheNotifications: "true"
  druid_auth_authenticator_BasicMetadataAuthenticator_cacheNotificationTimeout: "5000"

  # Authorization
  druid_auth_authorizers: '["BasicMetadataAuthorizer"]'
  druid_auth_authorizer_BasicMetadataAuthorizer_type: basic
  druid_auth_authorizer_BasicMetadataAuthorizer_enableCacheNotifications: "true"
  druid_auth_authorizer_BasicMetadataAuthorizer_cacheNotificationTimeout: "5000"

  # Escalator for internal communication
  druid_escalator_type: basic
  druid_escalator_internalClientUsername: druid_system
  druid_escalator_internalClientPassword: druid-internal-2026
  druid_escalator_authorizerName: BasicMetadataAuthorizer

  # Auth cache for bootstrap (critical!)
  druid_auth_basic_common_cacheDirectory: /opt/druid/var/auth-cache
  druid_auth_basic_common_pollingPeriod: "60000"
  druid_auth_basic_common_maxRandomDelay: "6000"

# Coordinator
coordinator:
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 768Mi
    limits:
      cpu: 500m
      memory: 1536Mi
  persistence:
    enabled: true
    storageClass: rook-ceph-block-enterprise
    size: 5Gi
  envVars:
    DRUID_XMX: 512m
    DRUID_XMS: 512m
    DRUID_MAXDIRECTMEMORYSIZE: 200m
    druid_coordinator_asOverlord_enabled: "true"
    druid_coordinator_asOverlord_overlordService: druid/overlord

# Broker
broker:
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 768Mi
    limits:
      cpu: 500m
      memory: 1536Mi
  envVars:
    DRUID_XMX: 384m
    DRUID_XMS: 384m
    DRUID_MAXDIRECTMEMORYSIZE: 400m
    druid_processing_buffer_sizeBytes: "50000000"
    druid_processing_numMergeBuffers: "2"
    druid_processing_numThreads: "1"

# Historical
historical:
  tiers:
    default:
      envVars:
        DRUID_XMX: 384m
        DRUID_XMS: 384m
        DRUID_MAXDIRECTMEMORYSIZE: 400m
        druid_processing_buffer_sizeBytes: "50000000"
        druid_processing_numMergeBuffers: "2"
        druid_processing_numThreads: "1"
        druid_server_tier: "tier_default"
        druid_segmentCache_locations: '[{"path":"/opt/druid/var/druid/segment-cache","maxSize":"1GiB"}]'
      resources:
        requests:
          cpu: 100m
          memory: 768Mi
        limits:
          cpu: 500m
          memory: 1536Mi
      persistence:
        size: 10Gi
        storageClass: rook-ceph-block-enterprise

# Disable MiddleManager (causes schema issues)
middleManager:
  enabled: false

# Disable Indexer (causes schema issues)
indexer:
  enabled: false

# Router (UI)
router:
  replicaCount: 1
  resources:
    requests:
      cpu: 50m
      memory: 256Mi
    limits:
      cpu: 250m
      memory: 512Mi
  envVars:
    DRUID_XMX: 128m
    DRUID_XMS: 128m
    DRUID_MAXDIRECTMEMORYSIZE: 100m
    druid_router_managementProxy_enabled: "true"
