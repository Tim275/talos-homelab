apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: druid

commonLabels:
  app.kubernetes.io/part-of: druid
  environment: staging

resources:
  - namespace.yaml
  - rbac.yaml
  - postgres.yaml
  - common-config.yaml
  - coordinator.yaml
  - overlord.yaml
  - broker.yaml
  - historical.yaml
  - middlemanager.yaml
  - router.yaml
  - httproute.yaml
