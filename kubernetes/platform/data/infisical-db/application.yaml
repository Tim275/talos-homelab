apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infisical-db
  namespace: argocd
  labels:
    app.kubernetes.io/managed-by: argocd
    platform.domain: data
spec:
  project: platform
  source:
    repoURL: https://github.com/Tim275/talos-homelab
    targetRevision: HEAD
    path: kubernetes/platform/data/infisical-db/overlays/prod
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
