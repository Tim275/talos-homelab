apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: kubernetes-workloads
  labels:
    release: kube-prometheus-stack
spec:
  groups:
    - name: kubernetes-workloads
      interval: 1m
      rules:
        - alert: PodCrashLooping
          expr: |
            max_over_time(kube_pod_container_status_waiting_reason{reason="CrashLoopBackOff"}[5m]) == 1
          for: 5m
          labels:
            severity: warning
            priority: P1
            component: application
            pagerduty_severity: warning
          annotations:
            summary: "Pod {{ $labels.namespace }}/{{ $labels.pod }} is crash looping"
            description: "Container {{ $labels.container }} in CrashLoopBackOff state for >5min"
            impact: "Service degradation"

        - alert: PodPendingTooLong
          expr: kube_pod_status_phase{phase="Pending"} == 1
          for: 10m
          labels:
            severity: warning
            component: kubernetes
          annotations:
            summary: "Pod {{ $labels.namespace }}/{{ $labels.pod }} pending >10min"
            description: "Likely scheduling failure or PVC unbound."

        - alert: ContainerOOMKilled
          expr: increase(kube_pod_container_status_terminated_reason{reason="OOMKilled"}[15m]) > 0
          labels:
            severity: warning
            component: kubernetes
          annotations:
            summary: "OOM kill: {{ $labels.namespace }}/{{ $labels.pod }}/{{ $labels.container }}"
            description: "Container OOM-killed in last 15min."

        - alert: PodNotReady
          # Exclude Failed: Job/CronJob pods go to Failed on completion (covered by
          # KubeJobFailed). Deployment pods in Failed get replaced and restart
          # (covered by PodCrashLooping). Only Pending/Unknown need 15min alerting.
          expr: kube_pod_status_phase{phase!~"Running|Succeeded|Failed"} == 1
          for: 15m
          labels:
            severity: warning
            priority: P1
            component: application
            pagerduty_severity: warning
          annotations:
            summary: "Pod {{ $labels.namespace }}/{{ $labels.pod }} not ready"
            description: "Pod in phase {{ $labels.phase }} for 15+ minutes"
            impact: "Service degradation"

        # PodOOMKilled removed — ContainerOOMKilled (above) already covers OOM kills
        # via increase(...[15m]) which is time-bounded. The last_terminated_reason
        # gauge stays at 1 forever after an OOMKill → causes permanent false fires.

    - name: p3.performance.info
      interval: 300s
      rules:
        - alert: HighPodRestartRate
          expr: rate(kube_pod_container_status_restarts_total[1h]) > 0.5
          for: 4h
          labels:
            severity: info
            priority: P3
            component: performance
          annotations:
            summary: "Pod {{ $labels.namespace }}/{{ $labels.pod }} restarting frequently"
            description: "Pod restarting {{ $value }} times per hour"
            impact: "Investigate application stability"
