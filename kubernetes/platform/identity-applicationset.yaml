apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: platform-identity
  namespace: argocd
  labels:
    enterprise.tier: platform
    enterprise.domain: identity
    team: timour
  annotations:
    argocd.argoproj.io/sync-wave: "17"
spec:
  goTemplate: true
  generators:
    # 🔐 IDENTITY PLATFORM: Pure Kustomize Control - only components in kustomization.yaml deployed
    - git:
        repoURL: https://github.com/Tim275/talos-homelab
        revision: HEAD
        files:
          - path: "kubernetes/platform/identity/kustomization.yaml"
  template:
    metadata:
      name: 'platform-identity'
      labels:
        managed-by: argocd
        domain: identity
        tier: platform
    spec:
      project: platform
      source:
        repoURL: https://github.com/Tim275/talos-homelab
        targetRevision: HEAD
        path: 'kubernetes/platform/identity'
        kustomize: {}
      destination:
        server: https://kubernetes.default.svc
      syncPolicy:
        automated: {}
        syncOptions:
          - CreateNamespace=true