apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: platform-developer
  namespace: argocd
  labels:
    enterprise.tier: platform
    enterprise.domain: developer
    team: timour
  annotations:
    argocd.argoproj.io/sync-wave: "18"
spec:
  goTemplate: true
  generators:
    # 👨‍💻 DEVELOPER PLATFORM: Pure Kustomize Control - only components in kustomization.yaml deployed
    - git:
        repoURL: https://github.com/Tim275/talos-homelab
        revision: HEAD
        files:
          - path: "kubernetes/platform/developer/kustomization.yaml"
  template:
    metadata:
      name: 'platform-developer'
      labels:
        managed-by: argocd
        domain: developer
        tier: platform
    spec:
      project: platform
      source:
        repoURL: https://github.com/Tim275/talos-homelab
        targetRevision: HEAD
        path: 'kubernetes/platform/developer'
        kustomize: {}
      destination:
        server: https://kubernetes.default.svc
      syncPolicy:
        automated: {}
        syncOptions:
          - CreateNamespace=true