# Placeholder — seal with real token after GitLab is running:
#
#   1. http://gitlab.timourhomelab.org → Admin → CI/CD → Runners → New runner
#   2. Copy the token (glrt-...)
#   3. kubectl create secret generic gitlab-runner-token \
#        --namespace gitlab-runner \
#        --from-literal=runner-registration-token="" \
#        --from-literal=runner-token="glrt-YOURTOKEN" \
#        --dry-run=client -o yaml | \
#        kubeseal --format yaml > runner-token-secret.yaml
#   4. Replace this file and commit
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: gitlab-runner-token
  namespace: gitlab-runner
spec:
  encryptedData:
    runner-registration-token: PLACEHOLDER
    runner-token: PLACEHOLDER
  template:
    metadata:
      name: gitlab-runner-token
      namespace: gitlab-runner
