## RenovateJob CR for Tim275/drova repo (Code-Repository)
## Different schedule than talos-homelab — separate parallel job.
apiVersion: renovate-operator.mogenius.com/v1alpha1
kind: RenovateJob
metadata:
  name: renovate-drova
  namespace: renovate-operator
spec:
  discoveryFilters:
    - Tim275/drova
  provider:
    name: github
    endpoint: ""
  image: ghcr.io/renovatebot/renovate:43.176.3   # renovate: docker=ghcr.io/renovatebot/renovate
  ## parallelism = "max projects scanned in parallel" (per Mogenius CRD semantics).
  ## Bei 1 Repo per Job ist parallelism>1 wirkungslos. Daher: 1.
  parallelism: 1
  ## Code-Repo Phase-2 Steady-State Schedule:
  ## - alle 2h zwischen 7-21 Uhr werktags Mo-Fr (UTC)
  ## - Bug-Fix-Bumps werden schnell PR'd (Stripe, gRPC, etc.)
  ## - Nachts + Wochenende KEIN Renovate-Noise (Dev-Workflow-friendly)
  ## - v43-Quirk wird mit häufigerer Frequenz wenn Backlog wächst (siehe Pitfall #10)
  schedule: "0 7-21/2 * * 1-5"
  secretRef: renovate-secret
  extraEnv:
    - name: LOG_LEVEL
      value: info
    - name: RENOVATE_GIT_AUTHOR
      value: "Renovate Bot <tim275@users.noreply.github.com>"
  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      cpu: 1500m
      memory: 2Gi
