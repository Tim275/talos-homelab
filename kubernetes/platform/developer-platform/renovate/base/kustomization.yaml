## Renovate via mogenius/renovate-operator
##
## OPERATOR: deployed in kubernetes/infrastructure/controllers/operators/base/
##           (Helm chart, Wave 0)
## CRs:       hier (Wave 20 — needs CRDs from operator)
##
## Renovate-Bot config (per-repo) lives in each repo's renovate.json.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: renovate-operator

commonAnnotations:
  argocd.argoproj.io/sync-wave: "20"

resources:
  - policy-exception.yaml
  - sealed-secret.yaml
  - oidc-sealed.yaml
  - oidc-securitypolicy.yaml
  - renovate-talos-homelab.yaml
  - renovate-drova.yaml
  ## orphan-pr-opener.yaml DISABLED 2026-05-12:
  ## Mit */15 cron-schedule erstellt Renovate selbst alle PRs.
  ## orphan-opener war fix für 2h-cron + v43-quirk. Bei stale branches
  ## (Wochen alt) öffnete er PRs mit massiv Konflikten (35 commits, +4749/-3376).
  ## Wenn v44+ released ODER cron auf 2h zurück → wieder enablen.
  # - orphan-pr-opener.yaml
