apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 📬 GRANULAR CONTROL: Messaging Platform Services Only
# Kafka, Schema Registry, Redpanda Console
namespace: argocd

resources:
  - ../platform-messaging.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: platform-messaging
  enterprise.granular: messaging-only