apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🛠️ GRANULAR CONTROL: All Platform Services
namespace: argocd

resources:
  - ../platform-data.yaml      # N8N, InfluxDB, PostgreSQL
  - ../platform-messaging.yaml # Kafka, Schema Registry, Redpanda Console
  - ../platform-developer.yaml # Backstage Developer Portal

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: platform-complete
  enterprise.granular: platform-services