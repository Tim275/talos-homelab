apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 💾 GRANULAR CONTROL: Data Platform Services Only
# N8N, InfluxDB, CloudBeaver, PostgreSQL
namespace: argocd

resources:
  - ../platform-data.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: platform-data
  enterprise.granular: data-only