apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🔧 GRANULAR CONTROL: Developer Platform Services Only
# Backstage Developer Portal
namespace: argocd

resources:
  - ../platform-developer.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/layer: platform-developer
  enterprise.granular: developer-only