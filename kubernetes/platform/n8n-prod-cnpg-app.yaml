apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-n8n-prod-cnpg
  namespace: argocd
  labels:
    platform.layer: data
    platform.component: n8n-database
    platform.critical: "true"
    enterprise.tier: platform
    app.kubernetes.io/managed-by: argocd
    team: timour
  annotations:
    argocd.argoproj.io/sync-wave: "12"
spec:
  project: default
  source:
    repoURL: https://github.com/Tim275/talos-homelab
    targetRevision: HEAD
    path: kubernetes/platform/data/n8n-prod-cnpg
  destination:
    server: https://kubernetes.default.svc
    namespace: n8n-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true