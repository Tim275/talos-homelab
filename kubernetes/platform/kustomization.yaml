apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd

      team: timour

commonAnnotations:
  argocd.argoproj.io/sync-wave: "15"

resources:
  - data-app.yaml
  - messaging-app.yaml
  # - drova-infra-app.yaml   # MIGRATED 2026-05-09 → applicationsets/tenants/drova-tenant.yaml
  - identity-app.yaml
  - gitlab-app.yaml
  - governance/tenants/
