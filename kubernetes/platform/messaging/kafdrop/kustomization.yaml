apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 13: Messaging Clusters (Kafdrop depends on Kafka)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "13"

namespace: kafka

helmCharts:
- name: kafdrop
  repo: https://ricsanfre.github.io/helm-charts/
  version: "0.1.0"
  releaseName: kafdrop
  namespace: kafka
  valuesFile: values.yaml

resources:
- application.yaml