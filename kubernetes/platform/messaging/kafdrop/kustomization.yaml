# 🚫 KAFDROP - DISABLED SERVICE
#
# This service is intentionally disabled to prevent automatic deployment.
# To enable Kafdrop Kafka UI, uncomment the configuration below.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 15: Platform UI Services (After Kafka)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "15"

namespace: kafka

# 🚫 NO RESOURCES - SERVICE DISABLED
resources: []

commonLabels:
  app.kubernetes.io/name: kafdrop
  app.kubernetes.io/component: kafka-ui
  app.kubernetes.io/part-of: messaging-platform
  service.status: disabled

# # To enable Kafdrop:
# # 1. Add resources below:
# resources:
# - namespace.yaml
# - deployment.yaml
# - service.yaml
# - ingress.yaml
# # 2. Remove from platform ApplicationSet exclude list