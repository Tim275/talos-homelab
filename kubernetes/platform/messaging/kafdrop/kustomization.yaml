# 🚫 KAFDROP - OPTIONAL SERVICE (COMMENTED OUT)
#
# Uncomment the following to enable Kafdrop Kafka UI:
#
# apiVersion: kustomize.config.k8s.io/v1beta1
# kind: Kustomization
#
# # 🌊 Sync Wave 15: Platform UI Services (After Kafka)
# commonAnnotations:
#   argocd.argoproj.io/sync-wave: "15"
#
# namespace: kafka
#
# resources:
# - namespace.yaml
# - deployment.yaml
# - service.yaml
# - ingress.yaml
#
# commonLabels:
#   app.kubernetes.io/name: kafdrop
#   app.kubernetes.io/component: kafka-ui
#   app.kubernetes.io/part-of: messaging-platform
#
# # To enable Kafdrop:
# # 1. Uncomment all lines above
# # 2. Add this to platform ApplicationSet:
# #    - path: "kubernetes/platform/messaging/kafdrop"
# # 3. Create the required YAML files (deployment, service, ingress)

# Placeholder to prevent ApplicationSet from failing
apiVersion: v1
kind: ConfigMap
metadata:
  name: kafdrop-placeholder
  namespace: kafka
  annotations:
    description: "Placeholder ConfigMap - Kafdrop is disabled. See kustomization.yaml to enable."
data:
  status: "disabled"
  instructions: "Edit kustomization.yaml to enable Kafdrop Kafka UI"