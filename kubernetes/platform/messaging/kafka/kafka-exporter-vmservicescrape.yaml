# 🎯 Kafka Exporter VMServiceScrape - Metrics scraping for Kafka monitoring
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: kafka-exporter-metrics
  namespace: kafka
  labels:
    app.kubernetes.io/name: kafka-exporter-vmservicescrape
    app.kubernetes.io/part-of: victoriametrics
spec:
  selector:
    matchLabels:
      strimzi.io/kind: KafkaExporter
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
