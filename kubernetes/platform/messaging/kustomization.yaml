apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 📨 PLATFORM MESSAGING DOMAIN - COMPONENT CONTROL
# =================================================
# Comment/Uncomment lines to enable/disable messaging platform components
# Uncommented = ✅ DEPLOYED | Commented = ❌ DISABLED

# 🎯 CHILD APPLICATIONS - APPEAR IN ARGOCD UI
# ===========================================
# ALL MESSAGING COMPONENTS TEMPORARILY DISABLED to save RAM for Wazuh
# Uncomment to re-enable:
# resources:
#   - kafka/application.yaml         # ❌ DISABLED - Strimzi Kafka (~4GB RAM)
#   - kafka-cfk/application.yaml     # ❌ DISABLED - Replaced by Strimzi
#   - redpanda-console/application.yaml  # ❌ DISABLED
#   - schema-registry/application.yaml   # ❌ DISABLED

# Empty resources - no messaging components deployed
resources: []

# 🏢 Domain Labels
labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      app.kubernetes.io/part-of: timour-homelab
      app.kubernetes.io/domain: messaging
      app.kubernetes.io/layer: platform
      team: timour

# 📊 Domain Annotations
commonAnnotations:
  argocd.argoproj.io/sync-wave: "16"
  enterprise.domain: "platform-messaging"
  gitops.control: "kustomize-component-level"
