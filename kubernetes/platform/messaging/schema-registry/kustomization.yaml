apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kafka

# Schema Registry DISABLED - See README.md for proper CFK Operator solution
# Bitnami images discontinued Aug 2025, use Confluent for Kubernetes instead
