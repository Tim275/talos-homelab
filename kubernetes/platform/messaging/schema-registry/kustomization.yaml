apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kafka

# Sync Wave 13: Schema Registry (depends on Kafka)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "13"

commonLabels:
  app.kubernetes.io/name: schema-registry
  app.kubernetes.io/component: schema-registry
  app.kubernetes.io/part-of: kafka

resources:
  # Apicurio Registry (Open Source, Confluent API kompatibel)
  - apicurio-registry.yaml
  # OLD: Confluent CFK (disabled - requires CFK Operator)
  # - schemaregistry.yaml
