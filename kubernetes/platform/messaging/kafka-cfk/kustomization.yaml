apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kafka

# 🌊 Sync Wave 12: Kafka Cluster (after CFK Operator wave 5)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "12"

commonLabels:
  app.kubernetes.io/name: kafka
  app.kubernetes.io/component: kafka
  app.kubernetes.io/part-of: messaging-platform
  app.kubernetes.io/managed-by: confluent-operator

resources:
  - namespace.yaml
  - kafka-jaas-secret.yaml  # JAAS config for Kafka auth
  - kraft-controller.yaml  # Deploy first (wave 11)
  - kafka-cluster.yaml  # Then Kafka brokers (wave 12)
