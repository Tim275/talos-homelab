resources:

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: messaging-

# 🌊 Sync Wave 13: Messaging Clusters (Redpanda Console depends on Kafka)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "13"

metadata:
  name: redpanda-console
  namespace: kafka

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: redpanda-console
  app.kubernetes.io/component: messaging
  app.kubernetes.io/part-of: platform

images:
  - name: redpandadata/console
    newTag: v3.2.2
