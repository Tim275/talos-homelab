apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: redpanda-console
  namespace: kafka

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - secret.yaml

commonLabels:
  app.kubernetes.io/name: redpanda-console
  app.kubernetes.io/component: messaging
  app.kubernetes.io/part-of: platform

images:
  - name: docker.redpanda.com/vectorized/console
    newTag: v2.3.8