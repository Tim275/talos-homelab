apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  app.kubernetes.io/managed-by: argocd

resources:
  - project.yaml
  - application-set.yaml
  # - kube-vip-application.yaml  # Disabled - conflicts with Talos native VIP
  # cloudflared - Moved to separate app