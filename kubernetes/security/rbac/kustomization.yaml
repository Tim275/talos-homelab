apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# RBAC Security Layer
# Authorization policies for all authentication types

namespace: default

commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"

resources:
  # OIDC User Authorization
  - oidc-users/

  # Future: Additional RBAC categories
  # - service-accounts/     # ServiceAccount-based access
  # - namespace-rbac/       # Namespace-scoped roles
  # - custom-roles/         # Custom ClusterRoles/Roles
