apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: security-foundation
  namespace: argocd
  labels:
    enterprise.tier: security
    enterprise.domain: foundation
    team: timour
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  goTemplate: true
  generators:
    # 🛡️ SECURITY FOUNDATION: Pure Kustomize Control - Comment/Uncomment = Enable/Disable
    - git:
        repoURL: https://github.com/Tim275/talos-homelab
        revision: HEAD
        files:
          - path: "kubernetes/security/foundation/kustomization.yaml"
  template:
    metadata:
      name: 'security-foundation'
      labels:
        managed-by: argocd
        domain: foundation
        tier: security
    spec:
      project: security
      source:
        repoURL: https://github.com/Tim275/talos-homelab
        targetRevision: HEAD
        path: 'kubernetes/security/foundation'
        kustomize: {}
      destination:
        server: https://kubernetes.default.svc
      syncPolicy:
        automated: {}
        syncOptions:
          - CreateNamespace=true