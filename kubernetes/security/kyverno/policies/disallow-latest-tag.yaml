apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: disallow-latest-tag
  annotations:
    policies.kyverno.io/title: Disallow Latest Tag
    policies.kyverno.io/category: s
    policies.kyverno.io/severity: medium
    policies.kyverno.io/subject: Pod
    policies.kyverno.io/description: >-
      The ':latest' tag is mutable and can lead to unexpected errors if the
      image changes. This policy validates that the image specifies a tag
      and that tag is not 'latest'.
spec:
  background: false
  rules:
    - name: validate-image-tag
      match:
        any:
          - resources:
              kinds:
                - Pod
      exclude:
        any:
          # Envoy Gateway Proxy uses hardcoded :latest for shutdown-manager sidecar
          # https://github.com/envoyproxy/gateway/issues/4567
          - resources:
              namespaces:
                - gateway
              selector:
                matchLabels:
                  app.kubernetes.io/managed-by: envoy-gateway
      validate:
        failureAction: Enforce
        message: "Using a mutable image tag e.g. 'latest' is not allowed."
        foreach:
          - list: "request.object.spec.containers"
            pattern:
              image: "!*:latest"
          - list: "request.object.spec.[initContainers, ephemeralContainers][]"
            pattern:
              image: "!*:latest"
