apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 30: Enterprise Identity & Access Management (LLDAP)
commonAnnotations:
  argocd.argoproj.io/sync-wave: "30"

namespace: lldap

# 🛡️ Enterprise Security & Identity Management
commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  app.kubernetes.io/component: identity-platform
  app.kubernetes.io/name: lldap
  team: timour
  service-owner: timour
  service-tier: security

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml