apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 30: Enterprise Identity & Access Management
commonAnnotations:
  argocd.argoproj.io/sync-wave: "30"

namespace: argocd

# 🛡️ Enterprise Identity Management Services
commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  app.kubernetes.io/component: identity-management
  team: timour
  service-tier: security

resources:
  # LDAP Directory Service
  - lldap/
  # Future: Vault, Keycloak, etc.