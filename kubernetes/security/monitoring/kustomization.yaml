apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 32: Enterprise Security Monitoring & SIEM
commonAnnotations:
  argocd.argoproj.io/sync-wave: "32"

namespace: argocd

# 🛡️ Enterprise Security Information & Event Management
commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  app.kubernetes.io/component: security-monitoring
  team: timour
  service-tier: security

resources:
  # Runtime Security Detection
  - falco/
  # SIEM & Log Analysis
  - siem/
  # Vulnerability Scanning
  - vulnerability-scanning/