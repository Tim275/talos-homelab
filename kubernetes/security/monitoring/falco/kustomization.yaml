apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 32: Falco Runtime Security
commonAnnotations:
  argocd.argoproj.io/sync-wave: "32"

namespace: falco-system

# 🛡️ Falco Runtime Security Detection
commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  app.kubernetes.io/component: runtime-security
  app.kubernetes.io/name: falco
  team: timour
  service-owner: timour
  service-tier: security

# Falco Runtime Security Detection Engine
# Resources will be added for Falco deployment
resources: []

# Future: Add Falco Helm chart or manifests:
# - DaemonSet for kernel module
# - Rules for threat detection
# - Sidekick for alerting integration