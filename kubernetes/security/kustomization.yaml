apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      team: timour

commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"

resources:
  - foundation/application.yaml
  - rbac/application.yaml
  - compliance/application.yaml
  - governance-app.yaml
