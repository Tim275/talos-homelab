apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      team: timour

commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"

resources:
  - foundation/application.yaml
  # PARKED 2026-05-09: Dead code from Authelia-era — used wrong prefix
  # 'oidc:admins' instead of 'oidc-grp:cluster-admins' (post-KC-migration).
  # Replaced by security/foundation/rbac/oidc-bindings.yaml.
  # Restore steps in security/rbac/PARKED.md.
  # - rbac/application.yaml
  - compliance/application.yaml
  - governance-app.yaml
