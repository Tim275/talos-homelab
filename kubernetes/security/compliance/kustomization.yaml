apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 31: Enterprise Security Compliance & Policy
commonAnnotations:
  argocd.argoproj.io/sync-wave: "31"

namespace: argocd

# 🛡️ Enterprise Compliance & Policy Management
commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  app.kubernetes.io/component: compliance-platform
  team: timour
  service-tier: security

resources:
  # CIS Kubernetes Benchmarks
  - cis-benchmarks/
  # OPA Gatekeeper Policies
  - gatekeeper-policies/
  # Pod Security Standards
  - pod-security-standards/