apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - kube-bench
  - kubescape
  # - tetragon  # PARKED 2026-05-13 — Talos kernel needs tracefs mount via machineconfig.
                # Files preserved in tetragon/ for re-enable after Talos patch:
                #   machine.kubelet.extraMounts:
                #     - destination: /sys/kernel/tracing
                #       type: tracefs, source: tracefs, options: [rw]
                # Plus node-reboot required. See tetragon-on-talos issues #1234.
