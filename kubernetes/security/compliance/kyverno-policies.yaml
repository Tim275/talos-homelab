apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: disallow-latest-tag
spec:
  failurePolicy: Ignore
  background: true
  rules:
    # Enforce since 2026-07-19, 0 violations after soak
    - name: require-image-tag
      match:
        any:
          - resources:
              kinds: ["Pod"]
      exclude:
        any:
          - resources:
              namespaces: ["kube-system", "kyverno", "argocd", "kubevirt"]
      validate:
        failureAction: Enforce
        message: "Using a mutable image tag like ':latest' is not allowed; pin a version."
        pattern:
          spec:
            containers:
              - image: "!*:latest"
---
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: audit-require-requests
spec:
  failurePolicy: Ignore
  background: true
  rules:
    - name: require-cpu-memory-requests
      match:
        any:
          - resources:
              kinds: ["Pod"]
      exclude:
        any:
          - resources:
              namespaces: ["kube-system", "kyverno", "argocd"]
      validate:
        failureAction: Audit
        message: "CPU and memory resource requests should be set."
        pattern:
          spec:
            containers:
              - resources:
                  requests:
                    cpu: "?*"
                    memory: "?*"
---
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: audit-require-limits
spec:
  failurePolicy: Ignore
  background: true
  rules:
    - name: require-memory-limits
      match:
        any:
          - resources:
              kinds: ["Pod"]
      exclude:
        any:
          - resources:
              namespaces: ["kube-system", "kyverno", "argocd"]
      validate:
        failureAction: Audit
        message: "A memory limit should be set (prevents node OOM + enables the memory alerts)."
        pattern:
          spec:
            containers:
              - resources:
                  limits:
                    memory: "?*"
---
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: verify-drova-signatures
spec:
  failurePolicy: Fail  # fail-closed: webhook outage must not silently bypass this gate
  webhookTimeoutSeconds: 30
  background: false
  rules:
    # Enforce since 2026-07-21
    - name: verify-cosign-keyless
      match:
        any:
          - resources:
              kinds: ["Pod"]
              namespaces: ["drova"]
      verifyImages:
        - imageReferences:
            - "ghcr.io/tim275/drova/*"
          failureAction: Enforce
          mutateDigest: false
          verifyDigest: false
          required: true
          attestors:
            - entries:
                - keyless:
                    subject: "https://github.com/Tim275/drova/.github/workflows/docker-build-push.yaml@*"
                    issuer: "https://token.actions.githubusercontent.com"
                    rekor:
                      url: https://rekor.sigstore.dev
