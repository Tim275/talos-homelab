apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 31: CIS Kubernetes Benchmarks
commonAnnotations:
  argocd.argoproj.io/sync-wave: "31"

namespace: kube-system

# 🛡️ CIS Kubernetes Security Benchmarks
commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  app.kubernetes.io/component: cis-benchmarks
  team: timour
  service-tier: security

# CIS Kubernetes Benchmarks Implementation
# Resources will be added as needed for compliance scanning
resources: []

# Future: Add kube-bench, kube-hunter, etc.