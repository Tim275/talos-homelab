apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 31: Pod Security Standards
commonAnnotations:
  argocd.argoproj.io/sync-wave: "31"

namespace: kube-system

# 🛡️ Kubernetes Pod Security Standards Implementation
commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  app.kubernetes.io/component: pod-security-standards
  team: timour
  service-tier: security

# Pod Security Standards enforcement via namespace labels
# Resources will be added for PSS configuration
resources: []

# Future: Add namespace PSS configurations:
# - Privileged: Allow everything (infrastructure namespaces)
# - Baseline: Prevent known privilege escalations
# - Restricted: Heavily restricted (application namespaces)