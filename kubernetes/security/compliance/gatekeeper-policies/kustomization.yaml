apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🌊 Sync Wave 31: OPA Gatekeeper Security Policies
commonAnnotations:
  argocd.argoproj.io/sync-wave: "31"

namespace: gatekeeper-system

# 🛡️ OPA Gatekeeper Policy Management
commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  app.kubernetes.io/component: policy-management
  team: timour
  service-tier: security

# OPA Gatekeeper Constraint Templates and Constraints
# Resources will be added as policies are defined
resources: []

# Future: Add constraint templates for:
# - Required security contexts
# - Resource limits enforcement
# - Image scanning requirements
# - Network policy enforcement