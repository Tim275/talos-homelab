apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # - n8n-dev.yaml      # MISSING FILE
  - n8n-prod.yaml       # Zero Trust network policy for N8N production
  - mtls-policies.yaml  # Per-namespace mTLS (n8n-prod, n8n-dev, keycloak, lldap)

labels:
  - pairs:
      security.category: network-policies
      managed-by: argocd
