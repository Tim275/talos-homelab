apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Drova tenant rate-limiting — co-located for discoverability.
# Envoy BackendTrafficPolicy: drova-login 5/min, drova-app 300/min.
resources:
  - policy.yaml
