apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - drova.yaml
  - n8n-dev.yaml
  - monitoring.yaml
  - storage.yaml

labels:
  - pairs:
      security.category: rate-limiting
      managed-by: argocd
