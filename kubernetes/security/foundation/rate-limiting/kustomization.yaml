apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - monitoring.yaml
  - services.yaml
  # - n8n-dev.yaml
  # - storage.yaml  # minio not deployed yet
  - tenants/drova

labels:
  - pairs:
      security.category: rate-limiting
      managed-by: argocd
