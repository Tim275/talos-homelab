apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# RBAC SECURITY FOUNDATION
# ===========================
# Role-Based Access Control policies for enterprise security

resources:
  # Future RBAC policies will be added here
  # - cluster-roles/
  # - service-accounts/
  # - role-bindings/

#  Security Foundation Labels
labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      app.kubernetes.io/component: rbac
      security.layer: foundation
      team: timour

# Security Annotations
commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"
  security.component: "rbac-foundation"
