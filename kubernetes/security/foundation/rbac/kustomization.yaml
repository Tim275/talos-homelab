apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


resources:
  - oidc-bindings.yaml      # OIDC group → cluster-admin via kubelogin
  # - cluster-roles/
  # - service-accounts/
  # - role-bindings/

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      app.kubernetes.io/component: rbac
      security.layer: foundation
      team: timour

commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"
  security.component: "rbac-foundation"
