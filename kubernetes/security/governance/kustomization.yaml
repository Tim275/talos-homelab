apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - kyverno/

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      app.kubernetes.io/domain: governance
      app.kubernetes.io/layer: security
      team: timour

commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"

