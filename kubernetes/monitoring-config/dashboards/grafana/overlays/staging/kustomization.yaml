# Grafana für Staging Edge-Cluster
# Components: arm64-arch + single-replica
# Kein OIDC/Keycloak (lokales basic-auth reicht für Pi-UI)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: grafana

resources:
  - ../../base

components:
  - ../../../../../../components/arm64-arch
  - ../../../../../../components/single-replica
