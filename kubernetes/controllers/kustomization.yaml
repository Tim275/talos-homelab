apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🎮 ENTERPRISE CONTROLLERS CONTROL - Netflix/Google Pattern
# ==========================================================
# Granular per-controller control. Enable/disable individual controllers:

namespace: argocd

# 🎯 Controller Services (Individual Control)
resources:
  # Core Controllers (Always Enabled)
  - ../applicationsets/infrastructure-controllers.yaml

  # Individual Controllers (Future granular control)
  # - ../applicationsets/controller-argocd.yaml
  # - ../applicationsets/controller-cert-manager.yaml
  # - ../applicationsets/controller-sealed-secrets.yaml
  # - ../applicationsets/controller-argo-rollouts.yaml

labels:
  - includeSelectors: true
    pairs:
      infrastructure.layer: controllers
      enterprise.tier: platform
      team: timour