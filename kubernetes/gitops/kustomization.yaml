apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
- project.yaml
- application-set.yaml
# Temporarily disabled apps to save memory during homelab startup:
# - ../apps/applicationsets