apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🏢 Enterprise ApplicationSets - Clean Structure
commonAnnotations:
  argocd.argoproj.io/sync-wave: "20"

namespace: argocd

resources:
  - quantlab.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  team: timour
  managed-by: timour