apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: audiobookshelf-prod
  namespace: argocd
  labels:
    app: audiobookshelf
    environment: production
    team: timour
spec:
  project: app-of-apps
  source:
    repoURL: https://github.com/Tim275/talos-homelab
    targetRevision: HEAD
    path: kubernetes/apps/audiobookshelf/environments/production
  destination:
    server: https://kubernetes.default.svc
    namespace: audiobookshelf-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: false  # Manual sync for production
    syncOptions:
      - CreateNamespace=true