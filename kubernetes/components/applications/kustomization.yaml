apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 🏢 Enterprise Applications - Clean Structure
commonAnnotations:
  argocd.argoproj.io/sync-wave: "25"

namespace: argocd

resources:
  - audiobookshelf-dev.yaml
  - audiobookshelf-prod.yaml
  - n8n-dev.yaml
  - n8n-prod.yaml
  - kafka-demo-dev.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: timour-homelab
  team: timour
  managed-by: timour