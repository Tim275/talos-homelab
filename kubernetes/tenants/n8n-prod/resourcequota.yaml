apiVersion: v1
kind: ResourceQuota
metadata:
  name: n8n-prod-quota
  namespace: n8n-prod
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 16Gi
    limits.cpu: "16"
    limits.memory: 32Gi
    persistentvolumeclaims: "20"
    services.loadbalancers: "0"
