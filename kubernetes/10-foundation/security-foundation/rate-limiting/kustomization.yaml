apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Cluster-wide rate-limit policies
  - monitoring.yaml
  - services.yaml
  # - n8n-dev.yaml
  # - storage.yaml  # minio not deployed yet
  # Tenant-specific (DAX pattern — grouped per tenant for discoverability)
  - tenants/drova

labels:
  - pairs:
      security.category: rate-limiting
      managed-by: argocd
